Процедура ОткрытьФормуВводаДаты(Форма, СтандартнаяОбработка) Экспорт
	
	СтандартнаяОбработка = Ложь;
	
	НачальнаяДата = ?(ЗначениеЗаполнено(Форма.ОригиналыДатаВозврата), Форма.ОригиналыДатаВозврата, ТекущаяДата());
	
	Оповещение = Новый ОписаниеОповещения("ПослеВводаДаты", Форма);
	ПоказатьВводДаты(Оповещение,НачальнаяДата,"Дата получения оригиналов",ЧастиДаты.Дата);
	
КонецПроцедуры

