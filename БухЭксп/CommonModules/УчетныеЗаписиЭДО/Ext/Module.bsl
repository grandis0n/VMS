
#Область СлужебныйПрограммныйИнтерфейс

#Область АвторизацияВСервисеЭДО

// Возвращаемое значение:
//  Структура:
//  * ЗашифрованныеМаркеры - см. КриптографияБЭДКлиентСервер.НовыйНаборДанныхДляРасшифровки
//  * РасшифрованныеМаркеры - см. КриптографияБЭДКлиентСервер.НовыйНаборРасшифрованныхДанных
//  * КонтекстДиагностики - см. ОбработкаНеисправностейБЭД.НовыйКонтекстДиагностики
//  * ИдентификаторыЭДО - Массив из Строка
//  * ПредставленияУчетныхЗаписейЭДО - Соответствие из КлючИЗначение:
//    ** Ключ - Строка - идентификатор ЭДО.
//    ** Значение - Строка - представление учетной записи ЭДО.
Функция НовыйРезультатАвторизацииВСервисеЭДО() Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("ЗашифрованныеМаркеры", Новый Соответствие);
	Результат.Вставить("РасшифрованныеМаркеры", Новый Соответствие);
	Результат.Вставить("КонтекстДиагностики", ОбработкаНеисправностейБЭД.НовыйКонтекстДиагностики());
	Результат.Вставить("ИдентификаторыЭДО", Новый Массив);
	Результат.Вставить("ПредставленияУчетныхЗаписейЭДО", Новый Соответствие);
	Возврат Результат;
	
КонецФункции

// Параметры:
//  РезультатыПолученияОтпечатковНаКлиенте - см. КриптографияБЭДКлиентСервер.НовыеРезультатыПолученияОтпечатков
//  ИдентификаторыЭДО - Массив из Строка
//  ВыбранныеСертификаты -  Массив из СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования
//  
// Возвращаемое значение:
//  См. НовыйРезультатАвторизацииВСервисеЭДО
Функция АвторизоватьсяВСервисеЭДО(РезультатыПолученияОтпечатковНаКлиенте, ИдентификаторыЭДО = Неопределено,
	ВыбранныеСертификаты = Неопределено) Экспорт
	
	РезультатАвторизации = НовыйРезультатАвторизацииВСервисеЭДО();
	
	КонтекстДиагностики = РезультатАвторизации.КонтекстДиагностики;
	
	Если ИдентификаторыЭДО = Неопределено Тогда
		ИдентификаторыЭДО = ИдентификаторыУчетныхЗаписейСпособаОбмена(
			Перечисления.СпособыОбменаЭД.ЧерезСервис1СЭДО);
	КонецЕсли;
	
	ИдентификаторыПоИнтеграциямЭДО = ИдентификаторыПоИнтеграциямЭДО(ИдентификаторыЭДО);
	Если ЗначениеЗаполнено(ИдентификаторыПоИнтеграциямЭДО.ИдентификаторыДляЛокальногоЭДО) Тогда
		
		ВидОперации = УчетныеЗаписиЭДОКлиентСервер.ВидОперацииАвторизацияВСервисеЭДО();
		РезультатыПолученияОтпечатков = КриптографияБЭД.ПолучитьОтпечаткиСертификатов(ВидОперации, КонтекстДиагностики,
			РезультатыПолученияОтпечатковНаКлиенте);
		
		ДанныеДляСинхронизации = СинхронизацияЭДО.ДанныеДляСинхронизации(РезультатыПолученияОтпечатков,
			КонтекстДиагностики, ИдентификаторыЭДО, ВыбранныеСертификаты);
		
		КлючиСинхронизации = ДанныеДляСинхронизации.КлючиСинхронизации;
		
		ОбщегоНазначенияКлиентСервер.ДополнитьСоответствие(
			РезультатАвторизации.ЗашифрованныеМаркеры,
			СинхронизацияЭДО.ЗашифрованныеМаркерыИзКлючейСинхронизации(КлючиСинхронизации));
			
		ОбщегоНазначенияКлиентСервер.ДополнитьСоответствие(
			РезультатАвторизации.РасшифрованныеМаркеры,
			СинхронизацияЭДО.РасшифрованныеМаркерыИзКлючейСинхронизации(КлючиСинхронизации));
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ИдентификаторыПоИнтеграциямЭДО.ИдентификаторыДляЦентраЭДО)
		И НастройкиЭДО.ИспользоватьИнтеграциюЦентраЭДО() Тогда
		ОбщийМодульУчетныеЗаписиЭДОИнтеграцияЦентраЭДО = ОбщегоНазначения.ОбщийМодуль(
			"УчетныеЗаписиЭДОИнтеграцияЦентраЭДО");
			
		РезультатАвторизацииВЦентреЭДО = ОбщийМодульУчетныеЗаписиЭДОИнтеграцияЦентраЭДО.АвторизоватьсяВСервисеЭДО(
			ИдентификаторыПоИнтеграциямЭДО.ИдентификаторыДляЦентраЭДО, 
			РезультатыПолученияОтпечатковНаКлиенте, ВыбранныеСертификаты);
		
		ОбработкаНеисправностейБЭДКлиентСервер.ДополнитьКонтекстДиагностики(
			КонтекстДиагностики, РезультатАвторизацииВЦентреЭДО.КонтекстДиагностики);
			
		ОбщегоНазначенияКлиентСервер.ДополнитьСоответствие(
			РезультатАвторизации.ЗашифрованныеМаркеры,
			РезультатАвторизацииВЦентреЭДО.ЗашифрованныеМаркеры);
			
	КонецЕсли;
	
	РезультатАвторизации.ПредставленияУчетныхЗаписейЭДО = ПредставленияУчетныхЗаписейЭДО(ИдентификаторыЭДО);
	
	Возврат РезультатАвторизации;
	
КонецФункции // АвторизацияВСервисеЭДО

#КонецОбласти

// Возвращает результат запроса с идентификатором ЭДО всех учетных записией
//
// Возвращаемое значение:
//  РезультатЗапроса
Функция СписокВсехУчетныхЗаписей() Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	УчетныеЗаписиЭДО.ИдентификаторЭДО
	|ИЗ
	|	РегистрСведений.УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО";
	
	УстановитьПривилегированныйРежим(Истина);
	РезультатЗапроса = Запрос.Выполнить();
	УстановитьПривилегированныйРежим(Ложь);
	
	Возврат РезультатЗапроса;
	
КонецФункции

// Возвращает описание запроса, в результате которого будут содержаться учетные записи.
// Запрос содержит следующие поля:
//  * ИдентификаторЭДО - Строка - идентификатор учетной записи
//  * НаименованиеУчетнойЗаписи - Строка - наименование учетной записи
//  * НазначениеУчетнойЗаписи - Строка - назначение учетной записи
//  * ПодробноеОписаниеУчетнойЗаписи - Строка - подробное описание учетной записи
//  * Организация - ОпределяемыйТип.Организация
//  * ОператорЭДО - Строка - идентификатор оператора электронного документооборота
//  * СпособОбменаЭД - ПеречислениеСсылка.СпособыОбменаЭД
//  * ПринятыУсловияИспользования - Булево - пользователь принял условия использования сервиса
//                                  электронного документооборота
//  * Сертификат - СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования
//  * ВозможенОбменБезПодписи - Булево
//  * ИмяФайлаСоглашенияНаРоуминг - Строка
//  * ДанныеСоглашенияНаРоуминг - ДвоичныеДанные
//  * ДатаСоглашенияНаРоуминг - Дата
//  * КаталогОбмена - Строка
//  * ПутьFTP - Строка
//  * УчетнаяЗаписьЭлектроннойПочты - СправочникСсылка.УчетныеЗаписиЭлектроннойПочты
//  * КодНалоговогоОргана - Строка.
//  * НеИспользоватьПриглашения - Булево - признак того, что перед началом ЭДО необходимо приглашение.
// 
// Параметры:
// 	ИмяВременнойТаблицы - Строка - таблица, в которую будет помещен результат запроса
// 	Отбор - см. НовыйОтборУчетныхЗаписей
// Возвращаемое значение:
// 	см. ОбщегоНазначенияБЭД.НовоеОписаниеЗапроса
Функция ЗапросУчетныхЗаписей(ИмяВременнойТаблицы, Отбор = Неопределено) Экспорт
	
	Если Отбор = Неопределено Тогда
		Отбор = НовыйОтборУчетныхЗаписей();
	КонецЕсли;
	
	ПоляУсловия = Новый Массив;
	Если Отбор.УчетныеЗаписи <> "" Тогда
		ПоляУсловия.Добавить(СтрШаблон("ИдентификаторЭДО В(%1)", Отбор.УчетныеЗаписи));
	КонецЕсли;
	Если Отбор.Организация <> "" Тогда
		ПоляУсловия.Добавить(СтрШаблон("Организация В (%1)", Отбор.Организация));
	КонецЕсли;
	Если Отбор.СпособОбмена <> "" Тогда
		ПоляУсловия.Добавить(СтрШаблон("СпособОбменаЭД В (%1)", Отбор.СпособОбмена));
	КонецЕсли;
	Если Отбор.Оператор <> "" Тогда
		ПоляУсловия.Добавить(СтрШаблон("ОператорЭДО В (%1)", Отбор.Оператор));
	КонецЕсли;
	Если Отбор.ИспользоватьЦентрЭДО <> Неопределено Тогда
		ПоляУсловия.Добавить(?(Отбор.ИспользоватьЦентрЭДО, "ИспользоватьЦентрЭДО", "ИспользоватьЦентрЭДО = ЛОЖЬ"));
	КонецЕсли;
	
	МассивПолейЗапроса = Новый Массив;
	МассивПолейЗапроса.Добавить("ИдентификаторЭДО");
	МассивПолейЗапроса.Добавить("НаименованиеУчетнойЗаписи");
	МассивПолейЗапроса.Добавить("НазначениеУчетнойЗаписи");
	МассивПолейЗапроса.Добавить("ПодробноеОписаниеУчетнойЗаписи");
	МассивПолейЗапроса.Добавить("Организация");
	МассивПолейЗапроса.Добавить("ОператорЭДО");
	МассивПолейЗапроса.Добавить("СпособОбменаЭД");
	МассивПолейЗапроса.Добавить("ПринятыУсловияИспользования");
	МассивПолейЗапроса.Добавить("СпособОбменаЭД В (&СпособыПрямогоОбмена) КАК ВозможенОбменБезПодписи");
	МассивПолейЗапроса.Добавить("ИмяФайлаСоглашенияНаРоуминг");
	МассивПолейЗапроса.Добавить("ДанныеСоглашенияНаРоуминг");
	МассивПолейЗапроса.Добавить("ДатаСоглашенияНаРоуминг");
	МассивПолейЗапроса.Добавить("КаталогОбмена");
	МассивПолейЗапроса.Добавить("ПутьFTP");
	МассивПолейЗапроса.Добавить("УчетнаяЗаписьЭлектроннойПочты");
	МассивПолейЗапроса.Добавить("КодНалоговогоОргана");
	МассивПолейЗапроса.Добавить("ИспользоватьЦентрЭДО");
	МассивПолейЗапроса.Добавить("НеИспользоватьПриглашения");
	МассивПолейЗапроса.Добавить("КодыТиповДокументовКОбмену");
	
	ПоляЗапроса = СтрСоединить(МассивПолейЗапроса, ",");
		
	ОписаниеЗапроса = ОбщегоНазначенияБЭД.НовоеОписаниеЗапроса();
	ОписаниеЗапроса.Текст = ШаблонЗапросаУчетныхЗаписей(ИмяВременнойТаблицы, ПоляЗапроса, ПоляУсловия);
	ОписаниеЗапроса.СлужебныеПараметры.Вставить("СпособыПрямогоОбмена", СинхронизацияЭДО.СпособыПрямогоОбмена());
	
	Возврат ОписаниеЗапроса;
	
КонецФункции

// Возвращает описание запроса, в результате которого будут содержаться сертификаты учетных записей.
// Запрос содержит следующие поля:
//  * ИдентификаторЭДО - Строка - идентификатор учетной записи
//  * Сертификат - СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования.
// 
// Параметры:
// 	ИмяВременнойТаблицы - Строка
// 	Отбор - см. НовыйОтборСертификатовУчетныхЗаписей
// Возвращаемое значение:
// 	См. ОбщегоНазначенияБЭД.НовоеОписаниеЗапроса
Функция ЗапросСертификатовУчетныхЗаписей(ИмяВременнойТаблицы, Отбор = Неопределено) Экспорт
	
	Если Отбор = Неопределено Тогда
		Отбор = НовыйОтборСертификатовУчетныхЗаписей();
	КонецЕсли;
	
	ПоляУсловия = Новый Массив;
	Если ЗначениеЗаполнено(Отбор.УчетныеЗаписи) Тогда
		ПоляУсловия.Добавить(СтрШаблон("СертификатыУчетныхЗаписей.ИдентификаторЭДО В(%1)", Отбор.УчетныеЗаписи));
	КонецЕсли;
	Если Отбор.ТолькоДействительные Тогда
		ПоляУсловия.Добавить("РАЗНОСТЬДАТ(&ТекущаяДата, СертификатыУчетныхЗаписей.ДействителенДо, МИНУТА) > 0");
	КонецЕсли;
	Если ЗначениеЗаполнено(Отбор.Сертификат) Тогда
		ПоляУсловия.Добавить(СтрШаблон("СертификатыУчетныхЗаписей.Сертификат В(%1)", Отбор.Сертификат));
	КонецЕсли;
	
	ТекстЗапроса = "ВЫБРАТЬ
	|	СертификатыУчетныхЗаписей.ИдентификаторЭДО КАК ИдентификаторЭДО,
	|	СертификатыУчетныхЗаписей.Сертификат КАК Сертификат,
	|	СертификатыУчетныхЗаписей.Доверенность КАК Доверенность,
	|	СертификатыУчетныхЗаписей.Должность КАК Должность
	|ПОМЕСТИТЬ ИмяВременнойТаблицы
	|ИЗ
	|	РегистрСведений.СертификатыУчетныхЗаписейЭДО КАК СертификатыУчетныхЗаписей
	|ГДЕ
	|&ПоляУсловия";
	
	ТекстЗапроса = ОбщегоНазначенияБЭД.ТекстЗапросаИзШаблона(ТекстЗапроса, ИмяВременнойТаблицы, "", ПоляУсловия);
	
	ОписаниеЗапроса = ОбщегоНазначенияБЭД.НовоеОписаниеЗапроса();
	ОписаниеЗапроса.Текст = ТекстЗапроса;
	ОписаниеЗапроса.СлужебныеПараметры.Вставить("ТекущаяДата", ТекущаяДатаСеанса());
	
	Возврат ОписаниеЗапроса;
	
КонецФункции

// Возвращает описание запроса для построения таблицы сертификатов на форме регистра сведений УчетныеЗаписиЭДО
// Параметры:
//  ИмяВременнойТаблицы - строка - имя временной таблицы откуда будут подобраны данные
// Возвращаемое значение:
// 	См. ОбщегоНазначенияБЭД.НовоеОписаниеЗапроса
Функция ЗапросСертификатовУчетнойЗаписиДляВыводаНаФорму(ИмяВременнойТаблицы) Экспорт
	
	ТекстЗапроса =
		"ВЫБРАТЬ
		|	СертификатыУчетныхЗаписей.Сертификат КАК Сертификат,
		|	СертификатыУчетныхЗаписей.Доверенность КАК Доверенность,
		|	СертификатыУчетныхЗаписей.Должность КАК Должность
		|ИЗ
		|	ИмяВременнойТаблицы КАК СертификатыУчетныхЗаписей
		|
		|УПОРЯДОЧИТЬ ПО
		|	Сертификат";
		
	ТекстЗапроса = ОбщегоНазначенияБЭД.ТекстЗапросаИзШаблона(ТекстЗапроса, ИмяВременнойТаблицы, "", "");
		
	ОписаниеЗапроса = ОбщегоНазначенияБЭД.НовоеОписаниеЗапроса();
	ОписаниеЗапроса.Текст = ТекстЗапроса;
	
	Возврат ОписаниеЗапроса;
	
КонецФункции

// Параметры:
//  ИдентификаторЭДО - см. РегистрСведений.СертификатыУчетныхЗаписейЭДО.ИдентификаторЭДО
// 
// Возвращаемое значение:
//  ТаблицаЗначений:
//  * Сертификат - см. РегистрСведений.СертификатыУчетныхЗаписейЭДО.Сертификат
//  * ДействителенДо - см. РегистрСведений.СертификатыУчетныхЗаписейЭДО.ДействителенДо
//  * Доверенность - см. РегистрСведений.СертификатыУчетныхЗаписейЭДО.Доверенность
//  * Должность - см. РегистрСведений.СертификатыУчетныхЗаписейЭДО.Должность
Функция СертификатыУчетнойЗаписи(ИдентификаторЭДО) Экспорт
	
	Возврат РегистрыСведений.СертификатыУчетныхЗаписейЭДО.СертификатыУчетнойЗаписи(ИдентификаторЭДО);
	
КонецФункции

// Возвращает учетные записи с непринятыми условиями использования.
// 
// Параметры:
// 	ИдентификаторыУчетныхЗаписей - Массив из Строка
// Возвращаемое значение:
// 	Массив из Строка - идентификаторы учетных записей
Функция УчетныеЗаписиЭДОБезПринятыхУсловийСервиса(ИдентификаторыУчетныхЗаписей) Экспорт
	
	Результат = Новый Массив;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	УчетныеЗаписиЭДО.ИдентификаторЭДО КАК ИдентификаторЭДО
		|ИЗ
		|	РегистрСведений.УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
		|ГДЕ
		|	УчетныеЗаписиЭДО.ИдентификаторЭДО В(&ИдентификаторыОрганизаций)
		|	И НЕ УчетныеЗаписиЭДО.ПринятыУсловияИспользования
		|	И НЕ УчетныеЗаписиЭДО.СпособОбменаЭД В (&СпособОбменаЭД)";
	
	Запрос.УстановитьПараметр("ИдентификаторыОрганизаций", ИдентификаторыУчетныхЗаписей);
	Запрос.УстановитьПараметр("СпособОбменаЭД", СинхронизацияЭДО.СпособыПрямогоОбмена());
	
	УстановитьПривилегированныйРежим(Истина);
	РезультатЗапроса = Запрос.Выполнить();
	УстановитьПривилегированныйРежим(Ложь);
	
	Если РезультатЗапроса.Пустой() Тогда
		Возврат Результат;
	КонецЕсли;
	
	Выборка = РезультатЗапроса.Выбрать();
	
	Пока Выборка.Следующий() Цикл
		Результат.Добавить(Выборка.ИдентификаторЭДО);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Возвращает описание запроса, в результате которого будут содержаться настройки учетных записей.
// 
// Параметры:
// 	ИмяВременнойТаблицы - Строка - таблица, в которую будет помещен результат запроса
// 	Отбор - см. НовыйОтборУчетныхЗаписей
// Возвращаемое значение:
// 	см. ОбщегоНазначенияБЭД.НовоеОписаниеЗапроса
Функция ЗапросНастроекУчетныхЗаписей(ИмяВременнойТаблицы, Отбор = Неопределено) Экспорт
	
	Если Отбор = Неопределено Тогда
		Отбор = НовыйОтборУчетныхЗаписей();
	КонецЕсли;
	
	ПоляУсловия = Новый Массив;
	Если Отбор.УчетныеЗаписи <> "" Тогда
		ПоляУсловия.Добавить(СтрШаблон("ИдентификаторЭДО В(&%1)", Отбор.УчетныеЗаписи));
	КонецЕсли;
	Если Отбор.Организация <> "" Тогда
		ПоляУсловия.Добавить(СтрШаблон("Организация = &%1", Отбор.Организация));
	КонецЕсли;
	Если Отбор.СпособОбмена <> "" Тогда
		ПоляУсловия.Добавить(СтрШаблон("СпособОбменаЭД = &%1", Отбор.СпособОбмена));
	КонецЕсли;
	
	ОписаниеЗапроса = ОбщегоНазначенияБЭД.НовоеОписаниеЗапроса();
	ОписаниеЗапроса.Текст = ШаблонЗапросаУчетныхЗаписей(ИмяВременнойТаблицы,
		"ИдентификаторЭДО, Организация, ОператорЭДО, СпособОбменаЭД,
		|КаталогОбмена, ПассивноеСоединениеFTP, ПортFTP, ПутьFTP, УчетнаяЗаписьЭлектроннойПочты", ПоляУсловия);
	
	Возврат ОписаниеЗапроса;
	
КонецФункции

// Возвращает учетные записи без доступных сертификатов.
// 
// Параметры:
// 	ОтпечаткиДоступныхУчетныхЗаписей - Массив из Строка
// Возвращаемое значение:
// 	Массив из Строка - идентификаторы учетных записей
Функция УчетныеЗаписиБезДоступныхСертификатов(ОтпечаткиДоступныхУчетныхЗаписей) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ
	|	СертификатыУчетныхЗаписей.ИдентификаторЭДО КАК ИдентификаторЭДО
	|ПОМЕСТИТЬ УчетныеЗаписиСДоступнымиСертификатами
	|ИЗ
	|	СертификатыУчетныхЗаписей КАК СертификатыУчетныхЗаписей
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ СертификатыДействующие КАК СертификатыДействующие
	|		ПО (СертификатыДействующие.Ссылка = СертификатыУчетныхЗаписей.Сертификат)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	УчетныеЗаписиЭДО.ИдентификаторЭДО КАК ИдентификаторОтправителя,
	|	ЛОЖЬ КАК ОбменБезПодписи
	|ПОМЕСТИТЬ НастройкиОтправкиЭлектронныхДокументовПоВидам
	|ИЗ
	|	УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
	|ГДЕ
	|	1 В
	|		(ВЫБРАТЬ ПЕРВЫЕ 1
	|			1
	|		ИЗ
	|			РегистрСведений.НастройкиОтправкиЭлектронныхДокументовПоВидам КАК НастройкиОтправкиЭлектронныхДокументовПоВидам
	|		ГДЕ
	|			НастройкиОтправкиЭлектронныхДокументовПоВидам.ИдентификаторОтправителя = УчетныеЗаписиЭДО.ИдентификаторЭДО
	|			И НЕ НастройкиОтправкиЭлектронныхДокументовПоВидам.ОбменБезПодписи)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	|	УчетныеЗаписиЭДО.ИдентификаторЭДО КАК ИдентификаторЭДО
	|ПОМЕСТИТЬ УчетныеЗаписиСНедоступнымиСертификатами
	|ИЗ
	|	УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ СертификатыУчетныхЗаписей КАК СертификатыУчетныхЗаписей
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ СертификатыКлючейЭлектроннойПодписиИШифрования КАК СертификатыКлючейЭлектроннойПодписиИШифрования
	|			ПО СертификатыУчетныхЗаписей.Сертификат = СертификатыКлючейЭлектроннойПодписиИШифрования.Ссылка
	|		ПО СертификатыУчетныхЗаписей.ИдентификаторЭДО = УчетныеЗаписиЭДО.ИдентификаторЭДО,
	|	Организации КАК Организации
	|
	|ОБЪЕДИНИТЬ
	|
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	УчетныеЗаписиЭДО.ИдентификаторЭДО
	|ИЗ
	|	УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
	|		ЛЕВОЕ СОЕДИНЕНИЕ СертификатыУчетныхЗаписей КАК СертификатыУчетныхЗаписей
	|		ПО УчетныеЗаписиЭДО.ИдентификаторЭДО = СертификатыУчетныхЗаписей.ИдентификаторЭДО
	|		ЛЕВОЕ СОЕДИНЕНИЕ НастройкиОтправкиЭлектронныхДокументовПоВидам КАК НастройкиОтправкиПоВидам
	|		ПО УчетныеЗаписиЭДО.ИдентификаторЭДО = НастройкиОтправкиПоВидам.ИдентификаторОтправителя
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Организации КАК Организации
	|		ПО УчетныеЗаписиЭДО.Организация = Организации.Ссылка
	|ГДЕ
	|	(НастройкиОтправкиПоВидам.ОбменБезПодписи = ЛОЖЬ
	|			ИЛИ НЕ УчетныеЗаписиЭДО.СпособОбменаЭД В (&СпособыПрямогоОбмена))
	|	И СертификатыУчетныхЗаписей.Сертификат ЕСТЬ NULL
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	УчетныеЗаписиСНедоступнымиСертификатами.ИдентификаторЭДО КАК ИдентификаторЭДО
	|ИЗ
	|	УчетныеЗаписиСНедоступнымиСертификатами КАК УчетныеЗаписиСНедоступнымиСертификатами
	|		ЛЕВОЕ СОЕДИНЕНИЕ УчетныеЗаписиСДоступнымиСертификатами КАК УчетныеЗаписиСДоступнымиСертификатами
	|		ПО (УчетныеЗаписиСДоступнымиСертификатами.ИдентификаторЭДО = УчетныеЗаписиСНедоступнымиСертификатами.ИдентификаторЭДО)
	|ГДЕ
	|	УчетныеЗаписиСДоступнымиСертификатами.ИдентификаторЭДО ЕСТЬ NULL";
	
	Отбор = КриптографияБЭД.НовыйОтборНедействующихИлиНедоступныхСертификатов();
	Отбор.ОпечаткиДоступныхСертификатов = "Отпечатки";
	Запросы = Новый Массив;
	Запросы.Добавить(КриптографияБЭД.ЗапросНеДействующихИлиНедоступныхСертификатов(
		"СертификатыКлючейЭлектроннойПодписиИШифрования", Отбор));
	Запросы.Добавить(ИнтеграцияЭДО.ЗапросДоступныхОрганизаций("Организации"));
	ОтборУчетныхЗаписей = НовыйОтборУчетныхЗаписей();
	Запросы.Добавить(ЗапросУчетныхЗаписей("УчетныеЗаписиЭДО", ОтборУчетныхЗаписей));
	Запросы.Добавить(ЗапросСертификатовУчетныхЗаписей("СертификатыУчетныхЗаписей"));
	
	ОтборДействующихСертификатов = КриптографияБЭД.НовыйОтборСертификатов();
	ОтборДействующихСертификатов.Отпечатки = "&Отпечатки";
	Запросы.Добавить(КриптографияБЭД.ЗапросДействующихСертификатов("СертификатыДействующие", ОтборДействующихСертификатов));
	
	ИтоговыйЗапрос = ОбщегоНазначенияБЭД.СоединитьЗапросы(Запрос, Запросы);
	СпособыПрямогоОбмена = СинхронизацияЭДО.СпособыПрямогоОбмена();
	ИтоговыйЗапрос.УстановитьПараметр("СпособыПрямогоОбмена", СпособыПрямогоОбмена);
	ИтоговыйЗапрос.УстановитьПараметр("Отпечатки", ОтпечаткиДоступныхУчетныхЗаписей);
	
	УстановитьПривилегированныйРежим(Истина);
	РезультатЗапроса = ИтоговыйЗапрос.Выполнить();
	УстановитьПривилегированныйРежим(Ложь);
	Выборка = РезультатЗапроса.Выбрать();
	
	УчетныеЗаписи = Новый Массив;
	Пока Выборка.Следующий() Цикл
		УчетныеЗаписи.Добавить(Выборка.ИдентификаторЭДО);
	КонецЦикла;
	
	Возврат УчетныеЗаписи;
	
КонецФункции

// Параметры:
//  Сертификаты - Массив из СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования
//  ИдентификаторыЭДО - Массив из Строка
// 
// Возвращаемое значение:
//  Соответствие из КлючИЗначение:
//  * Ключ - СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования
//  * Значение - Массив из Строка
Функция УчетныеЗаписиПоСертификатам(Сертификаты, ИдентификаторыЭДО = Неопределено) Экспорт
	
	Возврат РегистрыСведений.СертификатыУчетныхЗаписейЭДО.УчетныеЗаписиПоСертификатам(Сертификаты, ИдентификаторыЭДО);
	
КонецФункции

// Возвращает структуру для получения запроса учетных записей. См. ЗапросУчетныхЗаписей.
// 
// Возвращаемое значение:
// 	Структура:
// * УчетныеЗаписи - Строка - параметр или выражение для отбора по идентификаторам учетных записей
// * Организация - Строка - параметр или выражение для отбора по организации
// * СпособОбмена - Строка - параметр или выражение для отбора по способу обмена
// * Оператор - Строка - параметр или выражение для отбора по оператору
// * ИспользоватьЦентрЭДО - Неопределено,Булево - параметр или выражение для отбора по использованию обмена через центр ЭДО.
Функция НовыйОтборУчетныхЗаписей() Экспорт
	
	Отбор = Новый Структура;
	Отбор.Вставить("УчетныеЗаписи", "");
	Отбор.Вставить("Организация", "");
	Отбор.Вставить("СпособОбмена", "");
	Отбор.Вставить("Оператор", "");
	Отбор.Вставить("ИспользоватьЦентрЭДО", Неопределено);
	
	Возврат Отбор;
	
КонецФункции

// Возвращает структуру для получения запроса сертификатов учетных записей. См. ЗапросСертификатовУчетныхЗаписей.
// 
// Возвращаемое значение:
// 	Структура - Описание:
// * УчетныеЗаписи - Строка - параметр или выражение для отбора по идентификаторам учетных записей
// * ТолькоДействительные - Булево - отбирать только действительные сертификаты
// * Сертификат - Строка - параметр или выражение для отбора по сертификатам
Функция НовыйОтборСертификатовУчетныхЗаписей() Экспорт
	
	Отбор = Новый Структура;
	Отбор.Вставить("УчетныеЗаписи", "");
	Отбор.Вставить("ТолькоДействительные", Ложь);
	Отбор.Вставить("Сертификат", "");
	
	Возврат Отбор;
	
КонецФункции

// Изменяет свойство учетной записи "Версия конфигурации".
// 
// Параметры:
// 	ИдентификаторУчетнойЗаписи - Строка
// 	ВерсияКонфигурации - Строка
Процедура ИзменитьВерсиюКонфигурации(ИдентификаторУчетнойЗаписи, ВерсияКонфигурации) Экспорт
	
	МенеджерЗаписи = РегистрыСведений.УчетныеЗаписиЭДО.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.ИдентификаторЭДО = ИдентификаторУчетнойЗаписи;
	МенеджерЗаписи.Прочитать();

	Если МенеджерЗаписи.Выбран() И МенеджерЗаписи.ВерсияКонфигурации <> ВерсияКонфигурации Тогда

		МенеджерЗаписи.ВерсияКонфигурации = ВерсияКонфигурации;
		МенеджерЗаписи.Записать();

	КонецЕсли;
		
КонецПроцедуры

// Возвращает параметры учетной записи для обмена по FTP.
// 
// Параметры:
// 	УчетнаяЗапись - РегистрСведенийЗапись.УчетныеЗаписиЭДО
// Возвращаемое значение:
// 	Структура:
//    * ПутьFTP - Строка
//    * ПортFTP - Число
//    * Идентификатор - Строка
//    * ПассивноеСоединениеFTP - Булево
Функция ПараметрыУчетнойЗаписиДляОбменаПоFTP(УчетнаяЗапись) Экспорт
	
	ДанныеУчетнойЗаписи = Новый Структура;
	ДанныеУчетнойЗаписи.Вставить("ПутьFTP", УчетнаяЗапись.ПутьFTP);
	ДанныеУчетнойЗаписи.Вставить("ПортFTP", УчетнаяЗапись.ПортFTP);
	ДанныеУчетнойЗаписи.Вставить("Идентификатор", УчетнаяЗапись.ИдентификаторЭДО);
	ДанныеУчетнойЗаписи.Вставить("ПассивноеСоединениеFTP", УчетнаяЗапись.ПассивноеСоединениеFTP);
	
	Возврат ДанныеУчетнойЗаписи;
	
КонецФункции

// Параметры:
//  ИдентификаторЭДО - Строка
// 
// Возвращаемое значение:
//  Структура:
//  * Организация - Неопределено,ОпределяемыйТип.Организация
//  * ОператорЭДО - Строка
//  * АдресОрганизации - Строка
//  * АдресОрганизацииЗначение - Строка
//  * КодНалоговогоОргана - Строка
//  * ЭлектроннаяПочта - Строка
//  * НазначениеУчетнойЗаписи - Строка
//  * ПодробноеОписаниеУчетнойЗаписи - Строка
Функция ПараметрыУчетнойЗаписиДляОбменаЧерезОператора(ИдентификаторЭДО) Экспорт
	
	Возврат РегистрыСведений.УчетныеЗаписиЭДО.ПараметрыДляОбменаЧерезОператора(ИдентификаторЭДО);
	
КонецФункции

// Возвращаемое значение:
//  СписокЗначений из Строка
Функция СписокИдентификаторовУчетныхЗаписейОрганизацийСервисаЭДО() Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	УчетныеЗаписиЭДО.ИдентификаторЭДО КАК ИдентификаторЭДО,
		|	УчетныеЗаписиЭДО.НаименованиеУчетнойЗаписи КАК НаименованиеУчетнойЗаписи
		|ИЗ
		|	РегистрСведений.УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
		|ГДЕ
		|	УчетныеЗаписиЭДО.СпособОбменаЭД В (&СпособыОбменаЧерезОператора)";
	
	Запрос.УстановитьПараметр("СпособыОбменаЧерезОператора", СинхронизацияЭДО.СпособыОбменаЧерезОператора());
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	СписокИдентификаторов = Новый СписокЗначений;
	Пока Выборка.Следующий() Цикл
		СписокИдентификаторов.Добавить(Выборка.ИдентификаторЭДО, Выборка.НаименованиеУчетнойЗаписи);
	КонецЦикла; 
	
	Возврат СписокИдентификаторов;
	
КонецФункции

// Возвращает список идентификаторов и их представлений по организации и обособленным подразделениям для сервиса ЭДО.
//
// Параметры:
//   Организация - ОпределяемыйТип.Организация
//
// Возвращаемое значение:
//   СписокЗначений из Строка - список идентификаторов организации и их представлений.
//
Функция СписокИдентификаторовУчетныхЗаписейОрганизацииСервисаЭДО(Организация) Экспорт
	
	МассивЗначенийСвойств = ЗначенияСвойствУчетныхЗаписейОрганизацииСервисаЭДО(Организация,
		"ИдентификаторЭДО, НаименованиеУчетнойЗаписи");
	
	СписокИдентификаторов = Новый СписокЗначений;
	Для каждого ЗначенияСвойств Из МассивЗначенийСвойств Цикл
		СписокИдентификаторов.Добавить(ЗначенияСвойств.ИдентификаторЭДО, ЗначенияСвойств.НаименованиеУчетнойЗаписи);
	КонецЦикла; 
	
	Возврат СписокИдентификаторов;
	
КонецФункции

// Сохраняет данные соглашения на роуминг.
// 
// Параметры:
// 	ИдентификаторУчетнойЗаписи - Строка
// 	ДанныеСоглашенияНаРоуминг - ДвоичныеДанные
// 	ИмяФайла - Строка
// Возвращаемое значение:
// 	Структура:
// * Статус - Булево - Истина, если сохранение прошло успешно
// * ОписаниеОшибки - Строка
Функция ЗаписатьДанныеСоглашенияНаРоуминг(ИдентификаторУчетнойЗаписи, ДанныеСоглашенияНаРоуминг, ИмяФайла) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("Статус", Истина);
	Результат.Вставить("ОписаниеОшибки");
	
	НачатьТранзакцию();
	Попытка
		
		УстановитьПривилегированныйРежим(Истина);
		
		ОбщегоНазначенияБЭД.УстановитьУправляемуюБлокировку(
			"РегистрСведений.УчетныеЗаписиЭДО", Новый Структура("ИдентификаторЭДО", ИдентификаторУчетнойЗаписи));
		
		МенеджерЗаписи = РегистрыСведений.УчетныеЗаписиЭДО.СоздатьМенеджерЗаписи();
		МенеджерЗаписи.ИдентификаторЭДО = ИдентификаторУчетнойЗаписи;
		
		МенеджерЗаписи.Прочитать();
		Если Не МенеджерЗаписи.Выбран() Тогда
			Результат.Статус = Ложь;
			Результат.ОписаниеОшибки = НСтр("ru = 'Не найдена учетная запись организации'");
			ЗафиксироватьТранзакцию();
			Возврат Результат;
		КонецЕсли;
		
		МенеджерЗаписи.ИмяФайлаСоглашенияНаРоуминг = ИмяФайла;
		МенеджерЗаписи.ДанныеСоглашенияНаРоуминг = Новый ХранилищеЗначения(
			ДанныеСоглашенияНаРоуминг, Новый СжатиеДанных(9));
		МенеджерЗаписи.ДатаСоглашенияНаРоуминг = ТекущаяУниверсальнаяДата() + СмещениеСтандартногоВремени("GMT+3");
		МенеджерЗаписи.Записать();
		
		УстановитьПривилегированныйРежим(Ложь);
		
		ЗафиксироватьТранзакцию();
	Исключение
		ОтменитьТранзакцию();
		
		Результат.Статус = Ложь;
		Результат.ОписаниеОшибки = ОбработкаОшибок.КраткоеПредставлениеОшибки(ИнформацияОбОшибке());
		
		ЭлектронноеВзаимодействие.ОбработатьОшибку(НСтр("ru = 'Отправка приглашений ЭДО'"),
			ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
	КонецПопытки;
	
	Возврат Результат;
	
КонецФункции

// Удаляет данные соглашения на роуминг.
// 
// Параметры:
// 	ИдентификаторУчетнойЗаписи - Строка
// Возвращаемое значение:
// 	Булево
Функция УдалитьДанныеСоглашенияНаРоуминг(ИдентификаторУчетнойЗаписи) Экспорт
	
	Результат = Истина;
	
	НачатьТранзакцию();
	Попытка
		
		УстановитьПривилегированныйРежим(Истина);
		
		ОбщегоНазначенияБЭД.УстановитьУправляемуюБлокировку(
			"РегистрСведений.УчетныеЗаписиЭДО", Новый Структура("ИдентификаторЭДО", ИдентификаторУчетнойЗаписи));
		
		МенеджерЗаписи = РегистрыСведений.УчетныеЗаписиЭДО.СоздатьМенеджерЗаписи();
		МенеджерЗаписи.ИдентификаторЭДО = ИдентификаторУчетнойЗаписи;
		
		МенеджерЗаписи.Прочитать();
		Если Не МенеджерЗаписи.Выбран() Тогда
			Результат = Ложь;
			ЗафиксироватьТранзакцию();
			Возврат Результат;
		КонецЕсли;
		
		МенеджерЗаписи.ИмяФайлаСоглашенияНаРоуминг = "";
		МенеджерЗаписи.ДанныеСоглашенияНаРоуминг   = Новый ХранилищеЗначения(Неопределено);
		МенеджерЗаписи.ДатаСоглашенияНаРоуминг     = '00010101';
		МенеджерЗаписи.Записать();
		
		УстановитьПривилегированныйРежим(Ложь);
		
		ЗафиксироватьТранзакцию();
	Исключение
		ОтменитьТранзакцию();
		
		Результат = Ложь;
		
		ЭлектронноеВзаимодействие.ОбработатьОшибку(НСтр("ru = 'Отправка приглашений ЭДО'"),
			ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()),
			НСтр("ru = 'Не удалось удалить данные соглашения на роуминг для учетной записи организации'"));
	КонецПопытки;
	
	Возврат Результат;
	
КонецФункции

// Возвращает электронные адреса для уведомлений о статусе заявке на роуминг.
// 
// Параметры:
// 	Организация - ОпределяемыйТип.Организация
// Возвращаемое значение:
// 	Массив из Строка
Функция ЭлектронныеАдресаДляУведомленияОСтатусеЗаявкиНаРоуминг(Организация) Экспорт
	
	ДанныеУчетныхЗаписей = ЗначенияСвойствУчетныхЗаписейОрганизацииСервисаЭДО(Организация, "ЭлектроннаяПочтаДляУведомлений, EmailОрганизации");
	КонтактнаяИнформация = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъектов(
		ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(Организация),
		Перечисления.ТипыКонтактнойИнформации.АдресЭлектроннойПочты,,
		ТекущаяДатаСеанса());
	
	МассивEmailОрганизации = Новый Массив;
	МассивЭлектроннаяПочтаДляУведомлений = Новый Массив;
	МассивКонтактнаяИнформация = Новый Массив;
	Для каждого ДанныеУчетнойЗаписи Из ДанныеУчетныхЗаписей Цикл
		Если ЗначениеЗаполнено(ДанныеУчетнойЗаписи.EmailОрганизации)
			И МассивEmailОрганизации.Найти(ДанныеУчетнойЗаписи.EmailОрганизации) = Неопределено Тогда
			МассивEmailОрганизации.Добавить(ДанныеУчетнойЗаписи.EmailОрганизации);
		КонецЕсли;
		Если ЗначениеЗаполнено(ДанныеУчетнойЗаписи.ЭлектроннаяПочтаДляУведомлений)
			И МассивЭлектроннаяПочтаДляУведомлений.Найти(ДанныеУчетнойЗаписи.ЭлектроннаяПочтаДляУведомлений) = Неопределено Тогда
			МассивЭлектроннаяПочтаДляУведомлений.Добавить(ДанныеУчетнойЗаписи.ЭлектроннаяПочтаДляУведомлений);
		КонецЕсли;
	КонецЦикла;
	Для каждого ЭлементКонтактнойИнформации Из КонтактнаяИнформация Цикл
		Если ЗначениеЗаполнено(ЭлементКонтактнойИнформации.Представление)
			И МассивКонтактнаяИнформация.Найти(ЭлементКонтактнойИнформации.Представление) = Неопределено Тогда
			МассивКонтактнаяИнформация.Добавить(ЭлементКонтактнойИнформации.Представление);
		КонецЕсли;
	КонецЦикла; 
	
	ОбщегоНазначенияКлиентСервер.ДополнитьМассив(МассивEmailОрганизации, МассивЭлектроннаяПочтаДляУведомлений, Истина);
	ОбщегоНазначенияКлиентСервер.ДополнитьМассив(МассивEmailОрганизации, МассивКонтактнаяИнформация, Истина);
	
	Возврат МассивEmailОрганизации;
	
КонецФункции

// Сохраняет Email организации.
// 
// Параметры:
// 	ИдентификаторУчетнойЗаписи - Строка
// 	EmailОрганизации - Строка
Процедура ЗаписатьEmailОрганизации(ИдентификаторУчетнойЗаписи, EmailОрганизации) Экспорт
	
	НачатьТранзакцию();
	Попытка
		ОбщегоНазначенияБЭД.УстановитьУправляемуюБлокировку(
			"РегистрСведений.УчетныеЗаписиЭДО", Новый Структура("ИдентификаторЭДО", ИдентификаторУчетнойЗаписи));
		МенеджерЗаписиУчетнаяЗапись = РегистрыСведений.УчетныеЗаписиЭДО.СоздатьМенеджерЗаписи();
		МенеджерЗаписиУчетнаяЗапись.ИдентификаторЭДО = ИдентификаторУчетнойЗаписи;
		МенеджерЗаписиУчетнаяЗапись.Прочитать();
		Если Не МенеджерЗаписиУчетнаяЗапись.Выбран() Тогда
			МенеджерЗаписиУчетнаяЗапись.ИдентификаторЭДО = ИдентификаторУчетнойЗаписи;
		КонецЕсли;
		МенеджерЗаписиУчетнаяЗапись.EmailОрганизации = EmailОрганизации;
		МенеджерЗаписиУчетнаяЗапись.Записать();
		ЗафиксироватьТранзакцию();
	Исключение
		ОтменитьТранзакцию();
		ИмяСобытия = НСтр("ru = 'Запись email организации учетной записи электронного документооборота'",
			ОбщегоНазначения.КодОсновногоЯзыка());
		ЗаписьЖурналаРегистрации(ИмяСобытия, УровеньЖурналаРегистрации.Ошибка,,,
			ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
		ВызватьИсключение;
	КонецПопытки;

КонецПроцедуры

// Возвращает данные учетной записи.
// 
// Параметры:
// 	ИдентификаторУчетнойЗаписи - Строка
// 	КэшЦиклов - Соответствие из КлючИЗначение, Неопределено - Возвращаемый параметр, кэширует значение между циклами.
// Возвращаемое значение:
// 	Структура, Неопределено - данные учетной записи:
//  * НаименованиеУчетнойЗаписи - Строка - наименование учетной записи
//  * ИдентификаторОрганизации - Строка - идентификатор учетной записи
//  * Организация - ОпределяемыйТип.Организация
//  * ОператорЭДО - Строка - идентификатор оператора электронного документооборота
//  * СпособОбменаЭД - ПеречислениеСсылка.СпособыОбменаЭД
//  * ВидУчетнойЗаписи - ПеречислениеСсылка.ВидУчетнойЗаписиЭДО
Функция ДанныеУчетнойЗаписи(ИдентификаторУчетнойЗаписи, КэшЦиклов = Неопределено) Экспорт 
	
	Данные = Неопределено;
	
	КэшДанныеУчетныхЗаписей = Неопределено;
	Если КэшЦиклов <> Неопределено Тогда
		Если КэшЦиклов["КэшДанныеУчетныхЗаписей"] = Неопределено Тогда
			КэшЦиклов["КэшДанныеУчетныхЗаписей"] = Новый Соответствие;
		КонецЕсли;
		КэшДанныеУчетныхЗаписей = КэшЦиклов["КэшДанныеУчетныхЗаписей"];
	КонецЕсли;
	Если КэшДанныеУчетныхЗаписей <> Неопределено
		И КэшДанныеУчетныхЗаписей[ИдентификаторУчетнойЗаписи] <> Неопределено Тогда
		Возврат КэшДанныеУчетныхЗаписей[ИдентификаторУчетнойЗаписи];
	КонецЕсли;
	
	МодульЭлектронноеАктированиеЕИС = Неопределено;
	Если КэшЦиклов <> Неопределено И КэшЦиклов["ИспользоватьЭлектронноеАктированиеВЕИС"] <> Неопределено Тогда
		ИспользоватьЭлектронноеАктированиеВЕИС = КэшЦиклов["ИспользоватьЭлектронноеАктированиеВЕИС"];
	Иначе
		Если ОбщегоНазначения.ПодсистемаСуществует("ЭлектронноеВзаимодействие.ЭлектронноеАктированиеЕИС") Тогда
			МодульЭлектронноеАктированиеЕИС = ОбщегоНазначения.ОбщийМодуль("ЭлектронноеАктированиеЕИС");
			ИспользоватьЭлектронноеАктированиеВЕИС =
				МодульЭлектронноеАктированиеЕИС.ИспользоватьЭлектронноеАктированиеВЕИС();
		Иначе
			ИспользоватьЭлектронноеАктированиеВЕИС = Ложь;
		КонецЕсли;
		Если КэшЦиклов <> Неопределено Тогда
			КэшЦиклов["ИспользоватьЭлектронноеАктированиеВЕИС"] = ИспользоватьЭлектронноеАктированиеВЕИС;
		КонецЕсли;
	КонецЕсли;
	Если ИспользоватьЭлектронноеАктированиеВЕИС Тогда
		Если МодульЭлектронноеАктированиеЕИС = Неопределено Тогда
			МодульЭлектронноеАктированиеЕИС = ОбщегоНазначения.ОбщийМодуль("ЭлектронноеАктированиеЕИС");
		КонецЕсли;
		ДанныеУчетнойЗаписиЕИС = МодульЭлектронноеАктированиеЕИС.ДанныеУчетнойЗаписи(ИдентификаторУчетнойЗаписи);
		Если ЗначениеЗаполнено(ДанныеУчетнойЗаписиЕИС) Тогда
			Если КэшДанныеУчетныхЗаписей <> Неопределено Тогда
				КэшДанныеУчетныхЗаписей[ИдентификаторУчетнойЗаписи] = ДанныеУчетнойЗаписиЕИС;
			КонецЕсли;
			Возврат ДанныеУчетнойЗаписиЕИС;
		КонецЕсли;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
		|	УчетныеЗаписиЭДО.НаименованиеУчетнойЗаписи КАК НаименованиеУчетнойЗаписи,
		|	УчетныеЗаписиЭДО.Организация КАК Организация,
		|	УчетныеЗаписиЭДО.ОператорЭДО КАК ОператорЭДО,
		|	УчетныеЗаписиЭДО.СпособОбменаЭД КАК СпособОбменаЭД,
		|	УчетныеЗаписиЭДО.ИдентификаторЭДО КАК ИдентификаторОрганизации,
		|	УчетныеЗаписиЭДО.ВидУчетнойЗаписи КАК ВидУчетнойЗаписи
		|ИЗ
		|	РегистрСведений.УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
		|ГДЕ
		|	УчетныеЗаписиЭДО.ИдентификаторЭДО = &ИдентификаторЭДО";
	
	Запрос.УстановитьПараметр("ИдентификаторЭДО", ИдентификаторУчетнойЗаписи);
	
	УстановитьПривилегированныйРежим(Истина);
	РезультатЗапроса = Запрос.Выполнить();
	УстановитьПривилегированныйРежим(Ложь);
	
	ТаблицаРезультат = РезультатЗапроса.Выгрузить();
	
	Если ТаблицаРезультат.Количество() > 0 Тогда
		
		Данные = ОбщегоНазначения.СтрокаТаблицыЗначенийВСтруктуру(ТаблицаРезультат[0]);
		
	КонецЕсли;
	
	Если КэшДанныеУчетныхЗаписей <> Неопределено Тогда
		КэшДанныеУчетныхЗаписей[ИдентификаторУчетнойЗаписи] = Данные;
	КонецЕсли;
	
	Возврат Данные;
	
КонецФункции

// Возвращает таблицу учетных записей без сертификатов.
// 
// Возвращаемое значение:
// 	ТаблицаЗначений:
// * ИдентификаторЭДО - Строка
// * СообщениеЭДО - ДокументСсылка.СообщениеЭДО 
Функция НоваяТаблицаУчетныхЗаписейБезСертификатов() Экспорт
	
	УчетныеЗаписиБезСертификатов = Новый ТаблицаЗначений;
	УчетныеЗаписиБезСертификатов.Колонки.Добавить("ИдентификаторЭДО",
		Метаданные.РегистрыСведений.УчетныеЗаписиЭДО.Измерения.ИдентификаторЭДО.Тип);
	УчетныеЗаписиБезСертификатов.Колонки.Добавить("Сообщение", Новый ОписаниеТипов("ДокументСсылка.СообщениеЭДО"));
	
	Возврат УчетныеЗаписиБезСертификатов;
	
КонецФункции

// Возвращает ключ записи регистра сведений УчетныеЗаписиЭДО.
// 
// Параметры:
// 	ИдентификаторУчетнойЗаписи - Строка
// Возвращаемое значение:
// 	РегистрСведенийКлючЗаписи.УчетныеЗаписиЭДО
Функция КлючУчетнойЗаписи(ИдентификаторУчетнойЗаписи) Экспорт
	
	ЗначенияКлюча = Новый Структура("ИдентификаторЭДО", ИдентификаторУчетнойЗаписи);
	Возврат РегистрыСведений.УчетныеЗаписиЭДО.СоздатьКлючЗаписи(ЗначенияКлюча);
	
КонецФункции

// Создает учетную запись для обмена электронными документами.
// 
// Параметры:
// 	ОписаниеУчетнойЗаписи - см. УчетныеЗаписиЭДОКлиентСервер.НовоеОписаниеУчетнойЗаписи
// 	АдресИзменен - Булево
// Возвращаемое значение:
// 	Булево - учетная запись создана
Функция СоздатьУчетнуюЗапись(ОписаниеУчетнойЗаписи, АдресИзменен = Истина) Экспорт
	
	Возврат УчетныеЗаписиЭДОСлужебный.СоздатьУчетнуюЗапись(ОписаниеУчетнойЗаписи, АдресИзменен);

КонецФункции

// Параметры:
//  ИдентификаторУчетнойЗаписи - Строка
// 
// Возвращаемое значение:
//  Булево
Функция УдалитьУчетнуюЗапись(ИдентификаторУчетнойЗаписи) Экспорт
	
	Результат = Ложь;
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	УчетныеЗаписиЭДО.ИдентификаторЭДО КАК ИдентификаторЭДО,
		|	УчетныеЗаписиЭДО.СпособОбменаЭД КАК СпособОбменаЭД,
		|	УчетныеЗаписиЭДО.ИспользоватьЦентрЭДО КАК ИспользоватьЦентрЭДО
		|ИЗ
		|	РегистрСведений.УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
		|ГДЕ
		|	УчетныеЗаписиЭДО.ИдентификаторЭДО = &ИдентификаторЭДО";
	
	Запрос.УстановитьПараметр("ИдентификаторЭДО", ИдентификаторУчетнойЗаписи);
	
	НачатьТранзакцию();
	Попытка
		Поля = Новый Структура;
		Поля.Вставить("ИдентификаторЭДО", ИдентификаторУчетнойЗаписи);
		ОбщегоНазначенияБЭД.УстановитьУправляемуюБлокировку("РегистрСведений.УчетныеЗаписиЭДО",
			Поля);
		// Удаляем все под текущими правами пользователя.
		РезультатЗапроса = Запрос.Выполнить();
		ВыборкаУчетныеЗаписи = РезультатЗапроса.Выбрать();
		Если ВыборкаУчетныеЗаписи.Следующий() Тогда
			НаборЗаписей = РегистрыСведений.УчетныеЗаписиЭДО.СоздатьНаборЗаписей();
			НаборЗаписей.Отбор.ИдентификаторЭДО.Установить(ИдентификаторУчетнойЗаписи);
			НаборЗаписей.Записать();
			ПриУдаленииУчетнойЗаписи(ВыборкаУчетныеЗаписи.ИдентификаторЭДО,
				ВыборкаУчетныеЗаписи.СпособОбменаЭД,
				ВыборкаУчетныеЗаписи.ИспользоватьЦентрЭДО);
		КонецЕсли;
		
		Результат = Истина;
		ЗафиксироватьТранзакцию();
	Исключение
		ОтменитьТранзакцию();
		Информация = ИнформацияОбОшибке();
		
		Результат = Ложь;
		
		ЭлектронноеВзаимодействие.ОбработатьОшибку(НСтр("ru = 'Удаление учетной записи электронного документооборота'"),
			ОбработкаОшибок.ПодробноеПредставлениеОшибки(Информация));
			
	КонецПопытки;
	
	Возврат Результат;
	
КонецФункции

// Параметры:
//  НоваяУчетнаяЗапись - РегистрСведенийМенеджерЗаписи.УчетныеЗаписиЭДО,РегистрСведенийЗапись.УчетныеЗаписиЭДО
//  ОписаниеУчетнойЗаписи - см. УчетныеЗаписиЭДОКлиентСервер.НовоеОписаниеУчетнойЗаписи
Процедура ЗаполнитьДанныеУчетнойЗаписи(НоваяУчетнаяЗапись, ОписаниеУчетнойЗаписи) Экспорт
	
	УчетныеЗаписиЭДОСлужебный.ЗаполнитьДанныеУчетнойЗаписи(НоваяУчетнаяЗапись, ОписаниеУчетнойЗаписи);
	
КонецПроцедуры

// Параметры:
//  ИдентификаторУчетнойЗаписи - Строка
//  Сертификаты - СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования
//              - Массив из СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования
//  Замещать - Булево
//  Доверенности - Неопределено,Соответствие из КлючИЗначение:
//  * Ключ - СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования
//  * Значение - СправочникСсылка.МашиночитаемыеДоверенностиОрганизаций,СправочникСсылка.МЧД003
//  Должности - Неопределено,Соответствие из КлючИЗначение:
//  * Ключ - СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования
//  * Должность   - Строка.
Процедура ЗаписатьСертификатыУчетнойЗаписи(ИдентификаторУчетнойЗаписи, Знач Сертификаты, Замещать = Истина,
	Доверенности = Неопределено, Должности = Неопределено) Экспорт
	
	УчетныеЗаписиЭДОСлужебный.ЗаписатьСертификатыУчетнойЗаписи(ИдентификаторУчетнойЗаписи, Сертификаты, Замещать,
		Доверенности, Должности);
	
КонецПроцедуры

// Параметры:
//  Организация - ОпределяемыйТип.Организация
//  Сертификат - СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования
//  НовыйСертификат - СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования
// 
// Возвращаемое значение:
//  Массив из Структура:
//  * ИдентификаторЭДО - Строка
//  * Организация - ОпределяемыйТип.Организация
//  * НаименованиеУчетнойЗаписи - Строка
//  * ВерсияКонфигурации - Строка
//  * ОператорЭДО - Строка
//  * СпособОбменаЭД - ПеречислениеСсылка.СпособыОбменаЭД
//  * ЭлектроннаяПочтаДляУведомлений - Строка
//  * ОжидатьИзвещениеОПолучении - Булево
//  * УведомлятьОНовыхПриглашениях - Булево
//  * УведомлятьОбОтветахНаПриглашения - Булево
//  * УведомлятьОНовыхДокументах - Булево
//  * УведомлятьОНеОбработанныхДокументах - Булево
//  * УведомлятьОбОкончанииСрокаДействияСертификата -Булево
//  * КодНалоговогоОргана - Строка
//  * АдресОрганизации - Строка
//  * АдресОрганизацииЗначение - Строка
//  * НазначениеУчетнойЗаписи - Строка
//  * ПодробноеОписаниеУчетнойЗаписи - Строка
//  * ОбновитьНастройкиУведомлений - Булево
//  * ПринятыУсловияИспользования - Булево
//  * ВсеСертификаты - Массив из СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования
Функция УчетныеЗаписиЭДОДляОбновленияСертификата(Знач Организация, Знач Сертификат, Знач НовыйСертификат) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	УчетныеЗаписиЭДО.ИдентификаторЭДО КАК ИдентификаторЭДО,
	|	УчетныеЗаписиЭДО.Организация КАК Организация,
	|	УчетныеЗаписиЭДО.НаименованиеУчетнойЗаписи КАК НаименованиеУчетнойЗаписи,
	|	УчетныеЗаписиЭДО.ВерсияКонфигурации КАК ВерсияКонфигурации,
	|	УчетныеЗаписиЭДО.ОператорЭДО КАК ОператорЭДО,
	|	УчетныеЗаписиЭДО.СпособОбменаЭД КАК СпособОбменаЭД,
	|	УчетныеЗаписиЭДО.ЭлектроннаяПочтаДляУведомлений КАК ЭлектроннаяПочтаДляУведомлений,
	|	УчетныеЗаписиЭДО.ОжидатьИзвещениеОПолучении КАК ОжидатьИзвещениеОПолучении,
	|	УчетныеЗаписиЭДО.УведомлятьОНовыхПриглашениях КАК УведомлятьОНовыхПриглашениях,
	|	УчетныеЗаписиЭДО.УведомлятьОбОтветахНаПриглашения КАК УведомлятьОбОтветахНаПриглашения,
	|	УчетныеЗаписиЭДО.УведомлятьОНовыхДокументах КАК УведомлятьОНовыхДокументах,
	|	УчетныеЗаписиЭДО.УведомлятьОНеОбработанныхДокументах КАК УведомлятьОНеОбработанныхДокументах,
	|	УчетныеЗаписиЭДО.УведомлятьОбОкончанииСрокаДействияСертификата КАК УведомлятьОбОкончанииСрокаДействияСертификата,
	|	УчетныеЗаписиЭДО.КодНалоговогоОргана КАК КодНалоговогоОргана,
	|	УчетныеЗаписиЭДО.АдресОрганизации КАК АдресОрганизации,
	|	УчетныеЗаписиЭДО.АдресОрганизацииЗначение КАК АдресОрганизацииЗначение,
	|	УчетныеЗаписиЭДО.НазначениеУчетнойЗаписи КАК НазначениеУчетнойЗаписи,
	|	УчетныеЗаписиЭДО.ПодробноеОписаниеУчетнойЗаписи КАК ПодробноеОписаниеУчетнойЗаписи,
	|	УчетныеЗаписиЭДО.ОбновитьНастройкиУведомлений КАК ОбновитьНастройкиУведомлений,
	|	УчетныеЗаписиЭДО.ПринятыУсловияИспользования КАК ПринятыУсловияИспользования
	|ПОМЕСТИТЬ втУчетныеЗаписиЭДО
	|ИЗ
	|	РегистрСведений.УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.СертификатыУчетныхЗаписейЭДО КАК Сертификаты
	|		ПО УчетныеЗаписиЭДО.ИдентификаторЭДО = Сертификаты.ИдентификаторЭДО
	|			И (Сертификаты.Сертификат = &Сертификат)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.СертификатыУчетныхЗаписейЭДО КАК СертификатыНовый
	|		ПО УчетныеЗаписиЭДО.ИдентификаторЭДО = СертификатыНовый.ИдентификаторЭДО
	|			И (СертификатыНовый.Сертификат = &НовыйСертификат)
	|ГДЕ
	|	СертификатыНовый.Сертификат ЕСТЬ NULL
	|	И УчетныеЗаписиЭДО.Организация = &Организация
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	втУчетныеЗаписиЭДО.ИдентификаторЭДО КАК ИдентификаторЭДО,
	|	втУчетныеЗаписиЭДО.Организация КАК Организация,
	|	втУчетныеЗаписиЭДО.НаименованиеУчетнойЗаписи КАК НаименованиеУчетнойЗаписи,
	|	втУчетныеЗаписиЭДО.ВерсияКонфигурации КАК ВерсияКонфигурации,
	|	втУчетныеЗаписиЭДО.ОператорЭДО КАК ОператорЭДО,
	|	втУчетныеЗаписиЭДО.СпособОбменаЭД КАК СпособОбменаЭД,
	|	втУчетныеЗаписиЭДО.ЭлектроннаяПочтаДляУведомлений КАК ЭлектроннаяПочтаДляУведомлений,
	|	втУчетныеЗаписиЭДО.ОжидатьИзвещениеОПолучении КАК ОжидатьИзвещениеОПолучении,
	|	втУчетныеЗаписиЭДО.УведомлятьОНовыхПриглашениях КАК УведомлятьОНовыхПриглашениях,
	|	втУчетныеЗаписиЭДО.УведомлятьОбОтветахНаПриглашения КАК УведомлятьОбОтветахНаПриглашения,
	|	втУчетныеЗаписиЭДО.УведомлятьОНовыхДокументах КАК УведомлятьОНовыхДокументах,
	|	втУчетныеЗаписиЭДО.УведомлятьОНеОбработанныхДокументах КАК УведомлятьОНеОбработанныхДокументах,
	|	втУчетныеЗаписиЭДО.УведомлятьОбОкончанииСрокаДействияСертификата КАК УведомлятьОбОкончанииСрокаДействияСертификата,
	|	втУчетныеЗаписиЭДО.КодНалоговогоОргана КАК КодНалоговогоОргана,
	|	втУчетныеЗаписиЭДО.АдресОрганизации КАК АдресОрганизации,
	|	втУчетныеЗаписиЭДО.АдресОрганизацииЗначение КАК АдресОрганизацииЗначение,
	|	втУчетныеЗаписиЭДО.НазначениеУчетнойЗаписи КАК НазначениеУчетнойЗаписи,
	|	втУчетныеЗаписиЭДО.ПодробноеОписаниеУчетнойЗаписи КАК ПодробноеОписаниеУчетнойЗаписи,
	|	втУчетныеЗаписиЭДО.ОбновитьНастройкиУведомлений КАК ОбновитьНастройкиУведомлений,
	|	втУчетныеЗаписиЭДО.ПринятыУсловияИспользования КАК ПринятыУсловияИспользования
	|ИЗ
	|	втУчетныеЗаписиЭДО КАК втУчетныеЗаписиЭДО
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	СертификатыУчетныхЗаписейЭДО.ИдентификаторЭДО КАК ИдентификаторЭДО,
	|	СертификатыУчетныхЗаписейЭДО.Сертификат КАК Сертификат
	|ИЗ
	|	РегистрСведений.СертификатыУчетныхЗаписейЭДО КАК СертификатыУчетныхЗаписейЭДО
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ втУчетныеЗаписиЭДО КАК втУчетныеЗаписиЭДО
	|		ПО СертификатыУчетныхЗаписейЭДО.ИдентификаторЭДО = втУчетныеЗаписиЭДО.ИдентификаторЭДО";
	Запрос.УстановитьПараметр("Организация", Организация);
	Запрос.УстановитьПараметр("Сертификат", Сертификат);
	Запрос.УстановитьПараметр("НовыйСертификат", НовыйСертификат);
	
	Результаты = Запрос.ВыполнитьПакет();
	
	УчетныеЗаписи = Результаты[1].Выгрузить();
	УчетныеЗаписи.Колонки.Добавить("ВсеСертификаты");
	
	Сертификаты = Результаты[2].Выгрузить();
	Сертификаты.Индексы.Добавить("ИдентификаторЭДО");
	
	Для каждого УчетнаяЗапись Из УчетныеЗаписи Цикл
		
		ОтборПоУчетнойЗаписи = Новый Структура("ИдентификаторЭДО", УчетнаяЗапись.ИдентификаторЭДО);
		НайденныеСтроки = Сертификаты.НайтиСтроки(ОтборПоУчетнойЗаписи);
		
		УчетнаяЗапись.ВсеСертификаты = Новый Массив;
		УчетнаяЗапись.ВсеСертификаты.Добавить(НовыйСертификат);
		Для каждого СтрокаСертификата Из НайденныеСтроки Цикл
			УчетнаяЗапись.ВсеСертификаты.Добавить(СтрокаСертификата.Сертификат);
		КонецЦикла;
		
	КонецЦикла;
	
	Возврат ОбщегоНазначения.ТаблицаЗначенийВМассив(УчетныеЗаписи);
	
КонецФункции

// Параметры:
//  ОтпечаткиПоКонтекстам - см. КриптографияБЭДКлиентСервер.НовыеРезультатыПолученияОтпечатков
//  ИдентификаторыУчетныхЗаписейЭДО - Массив из Строка
// 
// Возвращаемое значение:
//  Соответствие из КлючИЗначение:
//  * Ключ - Строка - идентификатор учетной записи ЭДО.
//  * Значение - Массив из Строка - отпечатки сертификатов
Функция ОтпечаткиДействующихСертификатовПоУчетнымЗаписям(ОтпечаткиПоКонтекстам, ИдентификаторыУчетныхЗаписейЭДО) Экспорт
	
	ОтпечаткиПоУчетнымЗаписям = Новый Соответствие; // См. ОтпечаткиДействующихСертификатовПоУчетнымЗаписям
	
	ОписанияЗапросов = Новый Массив; // Массив из См. ОбщегоНазначенияБЭД.НовоеОписаниеЗапроса
	
	Отбор = КриптографияБЭД.НовыйОтборСертификатов();
	Отбор.Отпечатки = "&ОтпечаткиСертификатов";
	Отбор.ДоступныеТекущемуПользователю = Истина;
	Если Не ОтпечаткиПоКонтекстам.Облако.Доступность Тогда
		Отбор.Облачные = Ложь;
	КонецЕсли;
	ОписанияЗапросов.Добавить(КриптографияБЭД.ЗапросДействующихСертификатов("ДоступныеСертификаты", Отбор));
	
	Отбор = НовыйОтборСертификатовУчетныхЗаписей();
	Отбор.УчетныеЗаписи = "&ИдентификаторыУчетныхЗаписейЭДО";
	ОписанияЗапросов.Добавить(ЗапросСертификатовУчетныхЗаписей("СертификатыУчетныхЗаписей", Отбор));
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ДоступныеСертификаты.Отпечаток КАК Отпечаток,
		|	СертификатыУчетныхЗаписей.ИдентификаторЭДО КАК ИдентификаторЭДО
		|ИЗ
		|	СертификатыУчетныхЗаписей КАК СертификатыУчетныхЗаписей
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ДоступныеСертификаты КАК ДоступныеСертификаты
		|		ПО СертификатыУчетныхЗаписей.Сертификат = ДоступныеСертификаты.Ссылка";
	
	Запрос = ОбщегоНазначенияБЭД.СоединитьЗапросы(Запрос, ОписанияЗапросов);
	
	ВсеОтпечатки = КриптографияБЭД.ПолучитьВсеОтпечаткиСертификатов(ОтпечаткиПоКонтекстам);
	Запрос.УстановитьПараметр("ОтпечаткиСертификатов", ВсеОтпечатки);
	Запрос.УстановитьПараметр("ИдентификаторыУчетныхЗаписейЭДО", ИдентификаторыУчетныхЗаписейЭДО);
	
	РезультатЗапроса = Запрос.Выполнить();
	Если РезультатЗапроса.Пустой() Тогда
		Возврат ОтпечаткиПоУчетнымЗаписям;
	КонецЕсли;
	
	Выборка = РезультатЗапроса.Выбрать();
	Пока Выборка.Следующий() Цикл
		Отпечатки = ОтпечаткиПоУчетнымЗаписям[Выборка.ИдентификаторЭДО];
		Если Отпечатки = Неопределено Тогда
			Отпечатки = Новый Массив; // Массив из Строка
			ОтпечаткиПоУчетнымЗаписям.Вставить(Выборка.ИдентификаторЭДО, Отпечатки);
		КонецЕсли;
		Если Отпечатки.Найти(Выборка.Отпечаток) = Неопределено Тогда
			Отпечатки.Добавить(Выборка.Отпечаток);
		КонецЕсли;
	КонецЦикла;
	
	Возврат ОтпечаткиПоУчетнымЗаписям;
	
КонецФункции

// Параметры:
//  ИдентификаторыУчетныхЗаписейЭДО - Массив из Строка
// 
// Возвращаемое значение:
//  Соответствие из КлючИЗначение:
//  * Ключ - Строка - идентификатор учетной записи ЭДО.
//  * Значение - Массив из СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования
Функция ДействующиеСертификатыПоУчетнымЗаписям(ИдентификаторыУчетныхЗаписейЭДО) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	СертификатыУчетныхЗаписейЭДО.ИдентификаторЭДО КАК ИдентификаторЭДО,
		|	СертификатыУчетныхЗаписейЭДО.Сертификат КАК Сертификат
		|ИЗ
		|	РегистрСведений.СертификатыУчетныхЗаписейЭДО КАК СертификатыУчетныхЗаписейЭДО
		|ГДЕ
		|	СертификатыУчетныхЗаписейЭДО.ИдентификаторЭДО В (&ИдентификаторЭДО)
		|	И РАЗНОСТЬДАТ(&ТекущаяДата, СертификатыУчетныхЗаписейЭДО.ДействителенДо, МИНУТА) > 0";
	
	Запрос.УстановитьПараметр("ИдентификаторЭДО", ИдентификаторыУчетныхЗаписейЭДО);
	Запрос.УстановитьПараметр("ТекущаяДата", ТекущаяДатаСеанса());
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Выборка = РезультатЗапроса.Выбрать();
	
	СертификатыПоУчетнымЗаписям = Новый Соответствие;
	Пока Выборка.Следующий() Цикл
		Сертификаты = СертификатыПоУчетнымЗаписям[Выборка.ИдентификаторЭДО];
		Если Сертификаты = Неопределено Тогда
			Сертификаты = Новый Массив; // Массив из СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования
			СертификатыПоУчетнымЗаписям.Вставить(Выборка.ИдентификаторЭДО, Сертификаты);
		КонецЕсли;
		Сертификаты.Добавить(Выборка.Сертификат);
	КонецЦикла;
	
	Возврат СертификатыПоУчетнымЗаписям;
	
КонецФункции

// Параметры:
//  ИдентификаторыЭДО - Массив из Строка
// 
// Возвращаемое значение:
//  Соответствие из КлючИЗначение:
//  * Ключ - Строка - идентификатор учетной записи ЭДО.
//  * Значение - ОпределяемыйТип.Организация
Функция ОрганизацииУчетныхЗаписей(ИдентификаторыЭДО) Экспорт
	
	Результат = Новый Соответствие; // См. ОрганизацииУчетныхЗаписей
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	УчетныеЗаписиЭДО.ИдентификаторЭДО,
		|	УчетныеЗаписиЭДО.Организация
		|ИЗ
		|	РегистрСведений.УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
		|ГДЕ
		|	УчетныеЗаписиЭДО.ИдентификаторЭДО В (&ИдентификаторыЭДО)";
	
	Запрос.УстановитьПараметр("ИдентификаторыЭДО", ИдентификаторыЭДО);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Выборка = РезультатЗапроса.Выбрать();
	
	Пока Выборка.Следующий() Цикл
		Результат.Вставить(Выборка.ИдентификаторЭДО, Выборка.Организация);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Параметры:
//  ИдентификаторЭДО - Строка
// 
// Возвращаемое значение:
//  Неопределено,ОпределяемыйТип.Организация
Функция ОрганизацияУчетнойЗаписи(ИдентификаторЭДО) Экспорт
	ИдентификаторыЭДО = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ИдентификаторЭДО);
	ОрганизацииУчетныхЗаписей = ОрганизацииУчетныхЗаписей(ИдентификаторыЭДО);
	Возврат ОрганизацииУчетныхЗаписей[ИдентификаторЭДО];
КонецФункции

// Параметры:
//  ИдентификаторыЭДО - Массив из Строка
// 
// Возвращаемое значение:
//  Соответствие из КлючИЗначение:
//  * Ключ - Строка - идентификатор учетной записи ЭДО.
//  * Значение - ПеречислениеСсылка.СпособыОбменаЭД
Функция СпособыОбменаУчетныхЗаписей(ИдентификаторыЭДО) Экспорт
	
	Результат = Новый Соответствие; // См. СпособыОбменаУчетныхЗаписей
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	УчетныеЗаписиЭДО.ИдентификаторЭДО,
		|	УчетныеЗаписиЭДО.СпособОбменаЭД
		|ИЗ
		|	РегистрСведений.УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
		|ГДЕ
		|	УчетныеЗаписиЭДО.ИдентификаторЭДО В (&ИдентификаторыЭДО)";
	
	Запрос.УстановитьПараметр("ИдентификаторыЭДО", ИдентификаторыЭДО);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Выборка = РезультатЗапроса.Выбрать();
	
	Пока Выборка.Следующий() Цикл
		Результат.Вставить(Выборка.ИдентификаторЭДО, Выборка.СпособОбменаЭД);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Возвращает значения параметров учетных записей.
// Если список параметров не был передан, будут возвращены все реквизиты и ресурсы регистра.
//
// Параметры:
//  ИдентификаторыЭДО - Массив из Строка
//  СписокПараметров - Строка - Перечисление реквизитов (допускается обращение через "." и использование представлений).
// 
// Возвращаемое значение:
//  Соответствие из КлючИЗначение:
//  * Ключ - Строка - Идентификатор учетной записи ЭДО
//  * Значение - Структура:
//    ** Ключ - Строка - имя реквизита;
//    ** Значение - Произвольный - значение реквизита.
//
Функция ПараметрыУчетныхЗаписей(ИдентификаторыЭДО, СписокПараметров = "") Экспорт

	Результат = Новый Соответствие;
	СтрокаРеквизитов = "";

	Если ЗначениеЗаполнено(СписокПараметров) Тогда
		СтрокаРеквизитов = СписокПараметров;
	Иначе
		
		Для Каждого Ресурс Из Метаданные.РегистрыСведений.УчетныеЗаписиЭДО.Ресурсы Цикл
			СтрокаРеквизитов = СтрокаРеквизитов + Ресурс.Имя + ",";
		КонецЦикла;
		
		Для Каждого Реквизит Из Метаданные.РегистрыСведений.УчетныеЗаписиЭДО.Реквизиты Цикл
			СтрокаРеквизитов = СтрокаРеквизитов + Реквизит.Имя + ",";
		КонецЦикла;
		
		СтрокаРеквизитов = Лев(СтрокаРеквизитов, СтрДлина(СтрокаРеквизитов) - 1);
		
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ИдентификаторыЭДО", ИдентификаторыЭДО);
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ИдентификаторЭДО КАК ИдентификаторЭДО,
	|	&СтрокаРеквизитов
	|ИЗ
	|	РегистрСведений.УчетныеЗаписиЭДО
	|ГДЕ
	|	ИдентификаторЭДО В (&ИдентификаторыЭДО)
	|";
	
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&СтрокаРеквизитов", СтрокаРеквизитов);
	
	ТаблицаДанных = Запрос.Выполнить().Выгрузить();
	
	Для Каждого СтрокаТаблицы Из ТаблицаДанных Цикл
		
		СтруктураДанных = Новый Структура;
		
		Для Каждого КолонкаТаблицы Из ТаблицаДанных.Колонки Цикл
			
			Если КолонкаТаблицы.Имя = "ИдентификаторЭДО" Тогда
				Продолжить;
			КонецЕсли;
			
			СтруктураДанных.Вставить(КолонкаТаблицы.Имя, СтрокаТаблицы[КолонкаТаблицы.Имя]);
			
		КонецЦикла;
		
		Результат.Вставить(СтрокаТаблицы.ИдентификаторЭДО, СтруктураДанных);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Возвращаемое значение:
//  Структура:
//  * Организация - Неопределено,ОпределяемыйТип.Организация
//  * ЗаполнятьОператоровЭДО - Булево
//  * ЗаполнятьИдентификаторыОрганизации - Булево
//  * УчетнаяЗаписьЦентраЭДО - Неопределено,СправочникСсылка.УчетныеЗаписиЦентровЭДО
Функция НовыеПараметрыДанныхПоИдентификаторамДляРегистрацииВЭДО() Экспорт
	Параметры = Новый Структура;
	Параметры.Вставить("Организация", Неопределено);
	Параметры.Вставить("ЗаполнятьОператоровЭДО", Ложь);
	Параметры.Вставить("ЗаполнятьИдентификаторыОрганизации", Ложь);
	Параметры.Вставить("УчетнаяЗаписьЦентраЭДО", Неопределено);
	Возврат Параметры;
КонецФункции

// Возвращаемое значение:
//  ТаблицаЗначений:
//  * ИНН - Строка
//  * КПП - Строка
//  * ОГРН - Строка
//  * Сертификат - Строка
//  * СпособОбмена - ПеречислениеСсылка.СпособыОбменаЭД
//  * Наименование - Строка
//  * Идентификатор - Строка
Функция НовыйСписокОператоровДляРегистрацииВЭДО() Экспорт
	СписокОператоров = Новый ТаблицаЗначений;
	СписокОператоров.Колонки.Добавить("ИНН", ОбщегоНазначения.ОписаниеТипаСтрока(12));
	СписокОператоров.Колонки.Добавить("КПП", ОбщегоНазначения.ОписаниеТипаСтрока(9));
	СписокОператоров.Колонки.Добавить("ОГРН", ОбщегоНазначения.ОписаниеТипаСтрока(15));
	СписокОператоров.Колонки.Добавить("Сертификат", ОбщегоНазначения.ОписаниеТипаСтрока(28));
	СписокОператоров.Колонки.Добавить("СпособОбмена", Новый ОписаниеТипов("ПеречислениеСсылка.СпособыОбменаЭД"));
	СписокОператоров.Колонки.Добавить("Наименование", ОбщегоНазначения.ОписаниеТипаСтрока(150));
	СписокОператоров.Колонки.Добавить("Идентификатор", ОбщегоНазначения.ОписаниеТипаСтрока(10));
	Возврат СписокОператоров;
КонецФункции

// Возвращаемое значение:
//  Структура:
//  * ИдентификаторыОрганизации - Неопределено,СписокЗначений из Строка
//  * ОператорыЭДО - Неопределено
//                 - см. НовыйСписокОператоровДляРегистрацииВЭДО
//  * КонтекстДиагностики - см. ОбработкаНеисправностейБЭД.НовыйКонтекстДиагностики
Функция НовыеДанныеПоИдентификаторамДляРегистрацииВЭДО() Экспорт
	Данные = Новый Структура;
	Данные.Вставить("ИдентификаторыОрганизации", Неопределено); // СписокЗначений из Строка
	Данные.Вставить("ОператорыЭДО", Неопределено);
	Данные.Вставить("КонтекстДиагностики", ОбработкаНеисправностейБЭД.НовыйКонтекстДиагностики());
	Возврат Данные;
КонецФункции

// Параметры:
//  ПараметрыДанных - см. НовыеПараметрыДанныхПоИдентификаторамДляРегистрацииВЭДО
// 
// Возвращаемое значение:
//  См. НовыеДанныеПоИдентификаторамДляРегистрацииВЭДО
Функция ДанныеПоИдентификаторамДляРегистрацииВЭДО(ПараметрыДанных) Экспорт
	
	Если ЗначениеЗаполнено(ПараметрыДанных.УчетнаяЗаписьЦентраЭДО) Тогда
		ОбщийМодульУчетныеЗаписиЭДОВызовОблака = ОбщегоНазначения.ОбщийМодуль("УчетныеЗаписиЭДОИнтеграцияЦентраЭДО");
		Возврат ОбщийМодульУчетныеЗаписиЭДОВызовОблака.ДанныеПоИдентификаторамДляРегистрацииВЭДО(ПараметрыДанных);
	Иначе
		Возврат УчетныеЗаписиЭДОСлужебный.ДанныеПоИдентификаторамДляРегистрацииВЭДО(ПараметрыДанных);
	КонецЕсли;
	
КонецФункции

// Возвращаемое значение:
//  Структура:
// * УникальныйИдентификаторЗаявки1СЭДО - Строка
// * НаименованиеОрганизации - Строка
// * ИННОрганизации - Строка
// * КППОрганизации - Строка
// * ОГРНОрганизации - Строка
// * ИдентификаторОрганизации - Строка
// * ОператорЭДО - Строка
// * ОператорЭДОИд - Строка
// * Сертификат - Массив из Строка - набор сертификатов в формате base64.
// * ЭлектроннаяПочта - Строка
// * Фамилия - Строка
// * Имя - Строка
// * Отчество - Строка
// * ВидУчетнойЗаписи - ПеречислениеСсылка.ВидУчетнойЗаписиЭДО
Функция НовыеРеквизитыРегистрационногоПакета() Экспорт
	РеквизитыПакета = Новый Структура;
	РеквизитыПакета.Вставить("УникальныйИдентификаторЗаявки1СЭДО", "");
	РеквизитыПакета.Вставить("НаименованиеОрганизации", "");
	РеквизитыПакета.Вставить("ИННОрганизации", "");
	РеквизитыПакета.Вставить("КППОрганизации", "");
	РеквизитыПакета.Вставить("ОГРНОрганизации", "");
	РеквизитыПакета.Вставить("ИдентификаторОрганизации", "");
	РеквизитыПакета.Вставить("ОператорЭДО", "");
	РеквизитыПакета.Вставить("ОператорЭДОИд", "");
	РеквизитыПакета.Вставить("Сертификат", Новый Массив);
	РеквизитыПакета.Вставить("ЭлектроннаяПочта", "");
	РеквизитыПакета.Вставить("Фамилия", "");
	РеквизитыПакета.Вставить("Имя", "");
	РеквизитыПакета.Вставить("Отчество", "");
	РеквизитыПакета.Вставить("ВидУчетнойЗаписи", ПредопределенноеЗначение("Перечисление.ВидУчетнойЗаписиЭДО.ПустаяСсылка"));
	Возврат РеквизитыПакета;
КонецФункции

// Возвращаемое значение:
//  Структура:
//  * РеквизитыПакета - см. НовыеРеквизитыРегистрационногоПакета
//  * ДвоичныеДанныеДляОператораЭДО - Неопределено,ДвоичныеДанные
//  * ДвоичныеДанныеСоглашенияНаПодключениеЭДО - Неопределено,ДвоичныеДанные
//  * ОператорЭДОСертификат - Строка
//  * ДобавляемыйСертификат - СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования
//  * ПодписанныеДвоичныеДанныеСоглашенияНаПодключениеЭДО - Неопределено,ДвоичныеДанные
//  * ПодписанныеДвоичныеДанныеДляОператораЭДО - Неопределено,ДвоичныеДанные
//  * ЗашифрованныеДанныеОператораЭДО - Неопределено,ДвоичныеДанные
//  * ТекстОшибки - Строка
Функция НовыеДанныеДляРегистрационногоПакета1СЭДО() Экспорт
	ДанныеПакета = Новый Структура;
	ДанныеПакета.Вставить("РеквизитыПакета", Новый Структура);
	ДанныеПакета.Вставить("ДвоичныеДанныеДляОператораЭДО", Неопределено);
	ДанныеПакета.Вставить("ДвоичныеДанныеСоглашенияНаПодключениеЭДО", Неопределено);
	ДанныеПакета.Вставить("ОператорЭДОСертификат", "");
	ДанныеПакета.Вставить("ДобавляемыйСертификат", Справочники.СертификатыКлючейЭлектроннойПодписиИШифрования.ПустаяСсылка());
	ДанныеПакета.Вставить("ПодписанныеДвоичныеДанныеСоглашенияНаПодключениеЭДО", Неопределено);
	ДанныеПакета.Вставить("ПодписанныеДвоичныеДанныеДляОператораЭДО", Неопределено);
	ДанныеПакета.Вставить("ЗашифрованныеДанныеОператораЭДО", Неопределено);
	ДанныеПакета.Вставить("ТекстОшибки", "");
	Возврат ДанныеПакета;
КонецФункции

// Параметры:
//  ИдентификаторЭДО - Строка
// 
// Возвращаемое значение:
//  - Неопределено
//  - Структура:
//  * ИдентификаторЭДО - Строка
//  * Наименование - Строка
//  * Назначение - Строка
//  * ПодробноеОписание - Строка
Функция ИнформацияОбУчетнойЗаписи(ИдентификаторЭДО) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	УчетныеЗаписиЭДО.НазначениеУчетнойЗаписи КАК Назначение,
		|	УчетныеЗаписиЭДО.ПодробноеОписаниеУчетнойЗаписи КАК ПодробноеОписание,
		|	УчетныеЗаписиЭДО.НаименованиеУчетнойЗаписи КАК Наименование
		|ИЗ
		|	РегистрСведений.УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
		|ГДЕ
		|	УчетныеЗаписиЭДО.ИдентификаторЭДО = &ИдентификаторЭДО";
	
	Запрос.УстановитьПараметр("ИдентификаторЭДО", ИдентификаторЭДО);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Если РезультатЗапроса.Пустой() Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ИнформацияОбУчетнойЗаписиЭДО = Новый Структура;
	ИнформацияОбУчетнойЗаписиЭДО.Вставить("ИдентификаторЭДО", ИдентификаторЭДО);
	ИнформацияОбУчетнойЗаписиЭДО.Вставить("Наименование", "");
	ИнформацияОбУчетнойЗаписиЭДО.Вставить("Назначение", "");
	ИнформацияОбУчетнойЗаписиЭДО.Вставить("ПодробноеОписание", "");
	
	Выборка = РезультатЗапроса.Выбрать();
	Выборка.Следующий();
	ЗаполнитьЗначенияСвойств(ИнформацияОбУчетнойЗаписиЭДО, Выборка);
	
	Возврат ИнформацияОбУчетнойЗаписиЭДО;
	
КонецФункции

// Параметры:
//  ИдентификаторыЭДО - Массив из Строка
// 
// Возвращаемое значение:
//  Структура:
//  * ИдентификаторыДляЛокальногоЭДО - Массив из Строка
//  * ИдентификаторыДляЦентраЭДО - Массив из Строка
Функция ИдентификаторыПоИнтеграциямЭДО(ИдентификаторыЭДО) Экспорт
	
	ИдентификаторыДляЛокальногоЭДО = Новый Массив; // Массив из Строка
	ИдентификаторыДляЦентраЭДО = Новый Массив; // Массив из Строка
	
	ИдентификаторыПоИнтеграциямЭДО = Новый Структура;
	ИдентификаторыПоИнтеграциямЭДО.Вставить("ИдентификаторыДляЛокальногоЭДО", ИдентификаторыДляЛокальногоЭДО);
	ИдентификаторыПоИнтеграциямЭДО.Вставить("ИдентификаторыДляЦентраЭДО", ИдентификаторыДляЦентраЭДО);
	
	Выборка = РегистрыСведений.УчетныеЗаписиЭДО.ВыборкаПризнакаИспользоватьЦентрЭДО(ИдентификаторыЭДО);
	
	Пока Выборка.Следующий() Цикл
		Если Выборка.ИспользоватьЦентрЭДО Тогда
			ИдентификаторыДляЦентраЭДО.Добавить(Выборка.ИдентификаторЭДО);
		Иначе
			ИдентификаторыДляЛокальногоЭДО.Добавить(Выборка.ИдентификаторЭДО);
		КонецЕсли;
	КонецЦикла;
	
	Возврат ИдентификаторыПоИнтеграциямЭДО;
	
КонецФункции

// Параметры:
//  ИдентификаторЭДО - Строка
// 
// Возвращаемое значение:
//  Булево
Функция ИспользоватьЦентрЭДО(ИдентификаторЭДО) Экспорт
	
	Выборка = РегистрыСведений.УчетныеЗаписиЭДО.ВыборкаПризнакаИспользоватьЦентрЭДО(ИдентификаторЭДО);
	Если Выборка.Следующий() 
		И Выборка.ИспользоватьЦентрЭДО Тогда
		Возврат Истина;
	КонецЕсли;
	
	Возврат Ложь;
	
КонецФункции

// Возвращаемое значение:
//  Массив из Строка
Функция УчетныеЗаписиВРежимеПереходаВЦентрЭДО() Экспорт
	
	Возврат РегистрыСведений.УчетныеЗаписиЭДО.УчетныеЗаписиВРежимеПереходаВЦентрЭДО();
	
КонецФункции

// Возвращаемое значение:
//  Булево
Функция ЕстьУчетныеЗаписиЧерезЦентрЭДО() Экспорт
	Возврат РегистрыСведений.УчетныеЗаписиЭДО.ЕстьУчетныеЗаписиЧерезЦентрЭДО();
КонецФункции

// Параметры:
//  ИдентификаторыЭДО - Неопределено,Массив из Строка
// 
// Возвращаемое значение:
//  Булево
Функция ЕстьУчетныеЗаписиЧерезСервис1СЭДО(ИдентификаторыЭДО = Неопределено) Экспорт
	
	Возврат РегистрыСведений.УчетныеЗаписиЭДО.ЕстьУчетныеЗаписиЧерезСервис1СЭДО(ИдентификаторыЭДО);
	
КонецФункции

// Возвращаемое значение:
//  Булево
Функция ЕстьУчетныеЗаписиЛокальногоЭДО() Экспорт
	Возврат РегистрыСведений.УчетныеЗаписиЭДО.ЕстьУчетныеЗаписиЛокальногоЭДО();
КонецФункции

// Параметры:
//  ИдентификаторЭДО - Строка
// 
// Возвращаемое значение:
//  Булево
Функция ЕстьУчетнаяЗапись(ИдентификаторЭДО) Экспорт
	
	Возврат РегистрыСведений.УчетныеЗаписиЭДО.ЕстьУчетнаяЗапись(ИдентификаторЭДО);
	
КонецФункции

// Возвращаемое значение:
//  Булево
Функция ЕстьУчетныеЗаписиЭДО() Экспорт
	Возврат Не РегистрыСведений.УчетныеЗаписиЭДО.УчетныеЗаписиОтсутствуют();
КонецФункции

// Параметры:
//  Организация - ОпределяемыйТип.Организация
//  ИспользоватьЦентрЭДО - Неопределено,Булево
// 
// Возвращаемое значение:
//  Массив из Строка
Функция ИдентификаторыЭДОПоОрганизации(Организация, ИспользоватьЦентрЭДО = Неопределено) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	УчетныеЗаписиЭДО.ИдентификаторЭДО
		|ИЗ
		|	РегистрСведений.УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
		|ГДЕ
		|	УчетныеЗаписиЭДО.Организация = &Организация";
	
	Запрос.УстановитьПараметр("Организация", Организация);
	
	Если ИспользоватьЦентрЭДО <> Неопределено Тогда
		Запрос.Текст = Запрос.Текст + "
		|	И УчетныеЗаписиЭДО.ИспользоватьЦентрЭДО = &ИспользоватьЦентрЭДО";
		Запрос.УстановитьПараметр("ИспользоватьЦентрЭДО", ИспользоватьЦентрЭДО);
	КонецЕсли;
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Возврат РезультатЗапроса.Выгрузить().ВыгрузитьКолонку("ИдентификаторЭДО");
	
КонецФункции

// Параметры:
//  Организации - Массив из ОпределяемыйТип.Организация
//  ИспользоватьЦентрЭДО - Неопределено,Булево
// 
// Возвращаемое значение:
//  Соответствие из КлючИЗначение:
//  * Ключ - ОпределяемыйТип.Организация
//  * Значение - Массив из Строка
Функция ИдентификаторыУчетныхЗаписейПоОрганизациям(Организации, ИспользоватьЦентрЭДО = Неопределено) Экспорт
	
	Результат = ОбщегоНазначенияБЭДКлиентСервер.НовоеМножественноеСоответствие();
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	УчетныеЗаписиЭДО.ИдентификаторЭДО,
		|	УчетныеЗаписиЭДО.Организация
		|ИЗ
		|	РегистрСведений.УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
		|ГДЕ
		|	УчетныеЗаписиЭДО.Организация В (&Организации)";
	
	Запрос.УстановитьПараметр("Организации", Организации);
	
	Если ИспользоватьЦентрЭДО <> Неопределено Тогда
		Запрос.Текст = Запрос.Текст + "
		|	И УчетныеЗаписиЭДО.ИспользоватьЦентрЭДО = &ИспользоватьЦентрЭДО";
		Запрос.УстановитьПараметр("ИспользоватьЦентрЭДО", ИспользоватьЦентрЭДО);
	КонецЕсли;
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Выборка = РезультатЗапроса.Выбрать();
	Пока Выборка.Следующий() Цикл
		ОбщегоНазначенияБЭДКлиентСервер.ВставитьВоМножественноеСоответствие(Результат,
			Выборка.Организация, Выборка.ИдентификаторЭДО);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Параметры:
//  СпособОбмена - ПеречислениеСсылка.СпособыОбменаЭД
//  ИспользоватьЦентрЭДО - Неопределено,Булево
// 
// Возвращаемое значение:
//  Массив из Строка
Функция ИдентификаторыУчетныхЗаписейСпособаОбмена(СпособОбмена, ИспользоватьЦентрЭДО = Неопределено) Экспорт
	
	УчетныеЗаписи = Новый Массив; // Массив из Строка
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	УчетныеЗаписиЭДО.ИдентификаторЭДО
		|ИЗ
		|	РегистрСведений.УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
		|ГДЕ
		|	УчетныеЗаписиЭДО.СпособОбменаЭД = &СпособОбменаЭД
		|	И &УсловиеИспользоватьЦентрЭДО";
	
	Запрос.УстановитьПараметр("СпособОбменаЭД", СпособОбмена);
	Если ИспользоватьЦентрЭДО = Неопределено Тогда
		УсловиеИспользоватьЦентрЭДО = "ИСТИНА";
	Иначе
		УсловиеИспользоватьЦентрЭДО = ?(ИспользоватьЦентрЭДО, "", "НЕ ") + "УчетныеЗаписиЭДО.ИспользоватьЦентрЭДО";
	КонецЕсли;
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&УсловиеИспользоватьЦентрЭДО", УсловиеИспользоватьЦентрЭДО);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Выборка = РезультатЗапроса.Выбрать();
	Пока Выборка.Следующий() Цикл
		УчетныеЗаписи.Добавить(Выборка.ИдентификаторЭДО);
	КонецЦикла;
	
	Возврат УчетныеЗаписи;
	
КонецФункции

// Параметры:
//  Сертификаты - Массив из СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования
// 
// Возвращаемое значение:
//  Массив из Строка
Функция ИдентификаторыУчетныхЗаписейЧерезСервис1СЭДОСертификатов(Сертификаты) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		|	СертификатыУчетныхЗаписейЭДО.ИдентификаторЭДО КАК ИдентификаторЭДО
		|ИЗ
		|	РегистрСведений.СертификатыУчетныхЗаписейЭДО КАК СертификатыУчетныхЗаписейЭДО
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
		|		ПО СертификатыУчетныхЗаписейЭДО.ИдентификаторЭДО = УчетныеЗаписиЭДО.ИдентификаторЭДО
		|ГДЕ
		|	СертификатыУчетныхЗаписейЭДО.Сертификат В(&Сертификаты)
		|	И УчетныеЗаписиЭДО.СпособОбменаЭД = ЗНАЧЕНИЕ(Перечисление.СпособыОбменаЭД.ЧерезСервис1СЭДО)
		|	И НЕ УчетныеЗаписиЭДО.ИспользоватьЦентрЭДО";
	
	Запрос.УстановитьПараметр("Сертификаты", Сертификаты);
	РезультатЗапроса = Запрос.Выполнить();
	
	Возврат РезультатЗапроса.Выгрузить().ВыгрузитьКолонку("ИдентификаторЭДО");
	
КонецФункции

// Возвращаемое значение:
//  Строка
Функция ИдентификаторОшибкиНедействительныеСертификаты() Экспорт
	Возврат "НедействительныеСертификаты";
КонецФункции

// Параметры:
//  НаименованиеУчетнойЗаписи - Строка
//  ИдентификаторУчетнойЗаписи - Строка
//
// Возвращаемое значение:
//  см. ОбработкаНеисправностейБЭДКлиентСервер.НовоеОписаниеВидаОшибки
Функция ВидОшибкиНедействительныеСертификаты(НаименованиеУчетнойЗаписи, ИдентификаторУчетнойЗаписи) Экспорт

	ВидОшибки = ОбработкаНеисправностейБЭДКлиентСервер.НовоеОписаниеВидаОшибки();
	ВидОшибки.Идентификатор = ИдентификаторОшибкиНедействительныеСертификаты();
	ВидОшибки.ЗаголовокПроблемы = НСтр("ru = 'В учетной записи есть недействительные сертификаты'");
	ВидОшибки.ОписаниеПроблемы =
		ТекстСообщенияВУчетнойЗаписиНедействительныеСеритификаты(НаименованиеУчетнойЗаписи);
	ОписаниеРешения = НСтр("ru = '<a href = ""%1"">Удалите </a> недействительные сертификаты учетной записи'");
	ВидОшибки.ОписаниеРешения = СтрШаблон(ОписаниеРешения, "Удалите");
	ВидОшибки.ВыводитьСсылкуНаСписокОшибок = Ложь;
	ВидОшибки.ОбработчикиНажатия.Вставить("Удалите", "УчетныеЗаписиЭДОСлужебныйКлиент.ОткрытьФормуУчетнойЗаписи");
	ВидОшибки.ПараметрыОбработчиков.Вставить("Удалите", ИдентификаторУчетнойЗаписи);
	Возврат ВидОшибки;
	
КонецФункции

// Параметры:
//  НаименованиеУчетнойЗаписи - Строка
//
// Возвращаемое значение:
//  Строка
Функция ТекстСообщенияВУчетнойЗаписиНедействительныеСеритификаты(НаименованиеУчетнойЗаписи) Экспорт
	
	Возврат СтрШаблон(НСтр("ru = 'В учетной записи ""%1""
		|есть недействительные сертификаты.
		|Получение и отправка электронных документов по ней невозможны.'"), НаименованиеУчетнойЗаписи);
	
КонецФункции

// Параметры:
//  ДанныеДляСинхронизации - см. СинхронизацияЭДОСлужебный.ДанныеДляСинхронизации
//  ОбработанныеУчетныеЗаписи - Соответствие из КлючИЗначение:
//  * Ключ - Строка
//  * Значение - см. ДиагностикаЭДОКлиентСервер.НовоеОписаниеОбработаннойУчетнойЗаписи
Процедура ОбработатьОшибкиСинхронизации(ДанныеДляСинхронизации, ОбработанныеУчетныеЗаписи) Экспорт
	
	Для Каждого КлючИЗначение Из ДанныеДляСинхронизации.УчетныеЗаписиБезДоступаКСертификатам Цикл
		ОбработаннаяУчетнаяЗапись = ДиагностикаЭДОКлиентСервер.НовоеОписаниеОбработаннойУчетнойЗаписи();
		ОбработаннаяУчетнаяЗапись.Идентификатор = КлючИЗначение.Ключ;
		КоличествоСертификатов = КлючИЗначение.Значение.Количество();
		Если КоличествоСертификатов = 1 Тогда
			ШаблонОшибки = НСтр("ru = '<a href = ""%1"">Сертификат</a> доступен, но в поле ""Пользователь"" сертификата учетной записи указан пользователь, отличный от текущего'");
			ТекстОшибки = СтрШаблон(ШаблонОшибки, "Сертификаты");
		Иначе 
			ШаблонОшибки = НСтр("ru = '<a href = ""%1"">Сертификаты(%2)</a> доступны, но в поле ""Пользователь"" сертификата учетной записи указан пользователь, отличный от текущего'");
			ТекстОшибки = СтрШаблон(ШаблонОшибки, "Сертификаты", КоличествоСертификатов);
		КонецЕсли;
		ИнформацияОбОшибке = ОбработаннаяУчетнаяЗапись.ИнформацияОбОшибке;
		ИнформацияОбОшибке.ВидОшибки = ДиагностикаЭДОКлиентСервер.ВидОшибкиОтправкиИПолученияНетДоступаКСертификатам();
		ИнформацияОбОшибке.КраткоеПредставление = НСтр("ru = 'Нет доступа к сертификатам учетной записи'");
		ИнформацияОбОшибке.ПодробноеПредставление.Текст = СтроковыеФункции.ФорматированнаяСтрока(ТекстОшибки);
		Обработчик = Новый Структура;
		Обработчик.Вставить("ИмяПроцедуры", "ДиагностикаЭДОКлиент.ОткрытьСписокСертификатов");
		Обработчик.Вставить("Параметр", КлючИЗначение.Значение);
		ИнформацияОбОшибке.ПодробноеПредставление.ОбработчикиНажатия.Вставить("Сертификаты", Обработчик);
		ОбработанныеУчетныеЗаписи.Вставить(КлючИЗначение.Ключ, ОбработаннаяУчетнаяЗапись);
	КонецЦикла;
	
	Для Каждого УчетнаяЗапись Из ДанныеДляСинхронизации.УчетныеЗаписиБезДоступныхСертификатов Цикл
		ОбработаннаяУчетнаяЗапись = ДиагностикаЭДОКлиентСервер.НовоеОписаниеОбработаннойУчетнойЗаписи();
		ОбработаннаяУчетнаяЗапись.Идентификатор = УчетнаяЗапись;
		ТекстОшибки = НСтр("ru = 'Недоступен ни один личный сертификат из привязанных к учетной записи'");
		ИнформацияОбОшибке = ОбработаннаяУчетнаяЗапись.ИнформацияОбОшибке;
		ИнформацияОбОшибке.ВидОшибки = ДиагностикаЭДОКлиентСервер.ВидОшибкиОтправкиИПолученияНетДоступныхСертификатов();
		ИнформацияОбОшибке.КраткоеПредставление = НСтр("ru = 'Нет доступных сертификатов'");
		ИнформацияОбОшибке.ПодробноеПредставление.Текст = ТекстОшибки;
		ОбработанныеУчетныеЗаписи.Вставить(УчетнаяЗапись, ОбработаннаяУчетнаяЗапись);
	КонецЦикла;
	
КонецПроцедуры

// Параметры:
//  ИдентификаторЭДО - Строка
// 
// Возвращаемое значение:
//  Неопределено,Структура:
//  * ДатаПолученияДокументов - Число
//  * ДатаПолученияЭПД - Число
//  * ДатаПолученияПриглашений - Число
Функция СостояниеОбменаЧерезОператораЭДО(ИдентификаторЭДО) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	СостоянияОбменовЭДЧерезОператоровЭДО.ДатаПолученияЭД КАК ДатаПолученияЭД,
		|	СостоянияОбменовЭДЧерезОператоровЭДО.ДатаПолученияЭПД КАК ДатаПолученияЭПД,
		|	СостоянияОбменовЭДЧерезОператоровЭДО.ДатаПоследнегоПолученияПриглашений КАК ДатаПоследнегоПолученияПриглашений
		|ИЗ
		|	РегистрСведений.СостоянияОбменовЭДЧерезОператоровЭДО КАК СостоянияОбменовЭДЧерезОператоровЭДО
		|ГДЕ
		|	СостоянияОбменовЭДЧерезОператоровЭДО.ИдентификаторОрганизации = &ИдентификаторЭДО";
	
	Запрос.УстановитьПараметр("ИдентификаторЭДО", ИдентификаторЭДО);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Если Не Выборка.Следующий() Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Состояние = Новый Структура;
	Состояние.Вставить("ДатаПолученияДокументов", Выборка.ДатаПолученияЭД);
	Состояние.Вставить("ДатаПолученияЭПД", Выборка.ДатаПолученияЭПД);
	Состояние.Вставить("ДатаПолученияПриглашений", Выборка.ДатаПоследнегоПолученияПриглашений);
	Возврат Состояние;
	
КонецФункции

// См. УчетныеЗаписиЭДОСлужебный.ТребуетсяЗаполнитьДолжностьДляСертификата
Функция ТребуетсяЗаполнитьДолжностьДляСертификата(Организация, Сертификат) Экспорт
	
	Возврат УчетныеЗаписиЭДОСлужебный.ТребуетсяЗаполнитьДолжностьДляСертификата(Организация, Сертификат);
	
КонецФункции

#Область ДляВызоваИзДругихПодсистем

// См. СинхронизацияЭДО.ПриЗаполненииСписковСОграничениемДоступа
Процедура ПриЗаполненииСписковСОграничениемДоступа(Списки) Экспорт

	Списки.Вставить(Метаданные.РегистрыСведений.УчетныеЗаписиЭДО, Истина);
	
КонецПроцедуры

// См. СинхронизацияЭДО.ПриЗаполненииВидовОграниченийПравОбъектовМетаданных
Процедура ПриЗаполненииВидовОграниченийПравОбъектовМетаданных(Описание) Экспорт
	
	Описание = Описание + "
	|РегистрСведений.УчетныеЗаписиЭДО.Чтение.Организации
	|РегистрСведений.УчетныеЗаписиЭДО.Изменение.Организации
	|";
	
КонецПроцедуры

#Область ОбновлениеКонфигурации

// См. ЭлектронноеВзаимодействие.ПриДобавленииОбработчиковОбновления.
// 
// Параметры:
//  Обработчики - см. ОбновлениеИнформационнойБазы.НоваяТаблицаОбработчиковОбновления
Процедура ПриДобавленииОбработчиковОбновления(Обработчики) Экспорт

#Область Версия_1_9_16

#Область Константы_ДатаОтключенияAPIТакском_ОбработатьДанныеДляПереходаНаНовуюВерсию
	
	Обработчик = Обработчики.Добавить();
	Обработчик.Версия = "1.9.16.31";
	Обработчик.РежимВыполнения = "Отложенно";
	Обработчик.Процедура = "Константы.ДатаОтключенияAPIТакскомЭДО.ОбработатьДанныеДляПереходаНаНовуюВерсию";
	Обработчик.Идентификатор = Новый УникальныйИдентификатор("3795fcd8-af98-443b-85fa-f627fe8ffd0e");
	Обработчик.ПроцедураЗаполненияДанныхОбновления = "ЭлектронноеВзаимодействие.ЗаполнениеДанныхОбновления";
	Обработчик.ОчередьОтложеннойОбработки = 1;
	Обработчик.ПроцедураПроверки = "ОбновлениеИнформационнойБазы.ДанныеОбновленыНаНовуюВерсиюПрограммы";
	Обработчик.БлокируемыеОбъекты = "";
	Обработчик.Комментарий = НСтр("ru = '1С:Обмен с контрагентами: Установка даты отключения API из сохраненных данных патча'");
	
	Изменяемые = Новый Массив;
	Изменяемые.Добавить(Метаданные.Константы.ДатаОтключенияAPIТакскомЭДО.ПолноеИмя());
	Обработчик.ИзменяемыеОбъекты = СтрСоединить(Изменяемые, ",");
#КонецОбласти

#Область Константы_ДатаПереводаАбонентовИзТакском_ОбработатьДанныеДляПереходаНаНовуюВерсию
	
	Обработчик = Обработчики.Добавить();
	Обработчик.Версия = "1.9.16.31";
	Обработчик.РежимВыполнения = "Отложенно";
	Обработчик.Процедура = "Константы.ДатаПереводаАбонентовИзТакскомЭДО.ОбработатьДанныеДляПереходаНаНовуюВерсию";
	Обработчик.Идентификатор = Новый УникальныйИдентификатор("6cb5d807-e75e-456f-b54f-4b1b11bc28ca");
	Обработчик.ПроцедураЗаполненияДанныхОбновления = "ЭлектронноеВзаимодействие.ЗаполнениеДанныхОбновления";
	Обработчик.ОчередьОтложеннойОбработки = 1;
	Обработчик.ПроцедураПроверки = "ОбновлениеИнформационнойБазы.ДанныеОбновленыНаНовуюВерсиюПрограммы";
	Обработчик.БлокируемыеОбъекты = "";
	Обработчик.Комментарий = НСтр("ru = '1С:Обмен с контрагентами: Установка даты перевода абонентов поддержкой из сохраненных данных патча'");
	
	Изменяемые = Новый Массив;
	Изменяемые.Добавить(Метаданные.Константы.ДатаПереводаАбонентовИзТакскомЭДО.ПолноеИмя());
	Обработчик.ИзменяемыеОбъекты = СтрСоединить(Изменяемые, ",");
	
#КонецОбласти

#Область РегистрыСведений_УчетныеЗаписиПереведенныеИзТакском_ОбработатьДанныеДляПереходаНаНовуюВерсию

	Обработчик = Обработчики.Добавить();
	Обработчик.Версия = "1.9.16.31";
	Обработчик.РежимВыполнения = "Отложенно";
	Обработчик.ЗапускатьИВПодчиненномУзлеРИБСФильтрами = Истина;
	Обработчик.Процедура = "РегистрыСведений.УчетныеЗаписиПереведенныеИзТакскомЭДО.ОбработатьДанныеДляПереходаНаНовуюВерсию";
	Обработчик.Идентификатор = Новый УникальныйИдентификатор("ada22dca-e83f-45e8-a0ee-1ab535d13ec9");
	Обработчик.ПроцедураЗаполненияДанныхОбновления = "ЭлектронноеВзаимодействие.ЗаполнениеДанныхОбновления";
	Обработчик.ПроцедураПроверки = "ОбновлениеИнформационнойБазы.ДанныеОбновленыНаНовуюВерсиюПрограммы";
	Обработчик.Комментарий = НСтр("ru = '1С:Обмен с контрагентами: заполнение переведенных в хаб учетных записей из сохраненных данных патча.'");
	
	Изменяемые = Новый Массив;
	Изменяемые.Добавить(Метаданные.РегистрыСведений.УчетныеЗаписиПереведенныеИзТакскомЭДО.ПолноеИмя());
	Обработчик.ИзменяемыеОбъекты = СтрСоединить(Изменяемые, ",");

#КонецОбласти

#Область РегистрыСведений_ОшибкиПереводаАбонентовВХАБ_ОбработатьДанныеДляПереходаНаНовуюВерсию

	Обработчик = Обработчики.Добавить();
	Обработчик.Версия = "1.9.16.31";
	Обработчик.РежимВыполнения = "Отложенно";
	Обработчик.ЗапускатьИВПодчиненномУзлеРИБСФильтрами = Истина;
	Обработчик.Процедура = "РегистрыСведений.ОшибкиПереводаАбонентовВХАБЭДО.ОбработатьДанныеДляПереходаНаНовуюВерсию";
	Обработчик.Идентификатор = Новый УникальныйИдентификатор("7b1d3661-c802-4f2b-9402-bc328d0ffeb1");
	Обработчик.ПроцедураЗаполненияДанныхОбновления = "ЭлектронноеВзаимодействие.ЗаполнениеДанныхОбновления";
	Обработчик.ПроцедураПроверки = "ОбновлениеИнформационнойБазы.ДанныеОбновленыНаНовуюВерсиюПрограммы";
	Обработчик.Комментарий = НСтр("ru = '1С:Обмен с контрагентами: заполнение регистра ошибок перевода абонентов в ХАБ из сохраненных данных патча.'");
	
	Изменяемые = Новый Массив;
	Изменяемые.Добавить(Метаданные.РегистрыСведений.ОшибкиПереводаАбонентовВХАБЭДО.ПолноеИмя());
	Обработчик.ИзменяемыеОбъекты = СтрСоединить(Изменяемые, ",");

#КонецОбласти

#КонецОбласти

КонецПроцедуры

#КонецОбласти

#КонецОбласти

// Выполняет проверку существования учетной записи ЭДО.
// 
// Параметры:
//  Отбор - Структура:
//  *НазначениеУчетнойЗаписи - Строка
//  *КодНалоговогоОргана - Строка
//  *СпособОбменаЭД - ПеречислениеСсылка.СпособыОбменаЭД
//  *Организация - ОпределяемыйТип.Организация
// 
// Возвращаемое значение:
//  Булево - Учетная запись существует
Функция УчетнаяЗаписьСуществует(Знач Отбор) Экспорт

	ЗапросУчетныхЗаписей = ЗапросУчетныхЗаписей("УчетныеЗаписиЭДО");
	
	Запросы = Новый Массив;
	Запросы.Добавить(ЗапросУчетныхЗаписей);
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	УчетныеЗаписиЭДО.ИдентификаторЭДО КАК ИдентификаторЭДО
	|ИЗ
	|	УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
	|ГДЕ
	|	УчетныеЗаписиЭДО.НазначениеУчетнойЗаписи = &НазначениеУчетнойЗаписи
	|	И УчетныеЗаписиЭДО.КодНалоговогоОргана = &КодНалоговогоОргана
	|	И УчетныеЗаписиЭДО.СпособОбменаЭД = &СпособОбменаЭД
	|	И УчетныеЗаписиЭДО.Организация = &Организация";
	
	ИтоговыйЗапрос = ОбщегоНазначенияБЭД.СоединитьЗапросы(Запрос, Запросы);
	ИтоговыйЗапрос.УстановитьПараметр("НазначениеУчетнойЗаписи", Отбор.НазначениеУчетнойЗаписи);
    ИтоговыйЗапрос.УстановитьПараметр("КодНалоговогоОргана", Отбор.КодНалоговогоОргана);
	ИтоговыйЗапрос.УстановитьПараметр("СпособОбменаЭД", Отбор.СпособОбменаЭД);
	ИтоговыйЗапрос.УстановитьПараметр("Организация", Отбор.Организация);
	УстановитьПривилегированныйРежим(Истина);
	РезультатЗапроса = ИтоговыйЗапрос.Выполнить();
	УстановитьПривилегированныйРежим(Ложь);
	Возврат Не РезультатЗапроса.Пустой();
КонецФункции

// Параметры:
//  ИдентификаторУчетнойЗаписи - Строка
//  ПроизвестиПроверкуДействительныхСертификатовНаОтзыв - Булево
//
// Возвращаемое значение:
//  Булево
Функция ЕстьНедействительныеСертификаты(ИдентификаторУчетнойЗаписи,
	ПроизвестиПроверкуДействительныхСертификатовНаОтзыв = Ложь) Экспорт
	
	Если ПроизвестиПроверкуДействительныхСертификатовНаОтзыв Тогда
		ПроверкаНаОтзывДействительныхСертификатов(ИдентификаторУчетнойЗаписи);
	КонецЕсли;
	КоличествоПросроченныхСертификатов = КоличествоПросроченныхСертификатовУчетнойЗаписи(ИдентификаторУчетнойЗаписи);
	КоличествоОтозванныхСертификатов = КоличествоОтозванныхСертификатовУчетнойЗаписи(ИдентификаторУчетнойЗаписи);
	Возврат КоличествоПросроченныхСертификатов + КоличествоОтозванныхСертификатов > 0;
	
КонецФункции

// Возвращаемое значение:
//  Массив из СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования
Функция ОтозванныеСертификаты() Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	СертификатыУчетныхЗаписейЭДО.Сертификат КАК Сертификат
		|ИЗ
		|	РегистрСведений.СертификатыУчетныхЗаписейЭДО КАК СертификатыУчетныхЗаписейЭДО";
	Сертификаты = Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Сертификат");
	Возврат ИнтеграцияБСПБЭД.ОтозванныеСертификаты(Сертификаты);
	
КонецФункции

// см. ОбменСКонтрагентами.ДанныеУчетныхЗаписейЭДООрганизации
//
Функция ДанныеУчетныхЗаписейЭДООрганизации(Организация) Экспорт
	
	УчетныеЗаписи = ЗначенияСвойствУчетныхЗаписейОрганизацииСервисаЭДО(Организация,
		"ИдентификаторЭДО, НаименованиеУчетнойЗаписи, НазначениеУчетнойЗаписи, СпособОбменаЭД");
	
	ИдентификаторыЭДО = ОбщегоНазначения.ВыгрузитьКолонку(УчетныеЗаписи, "ИдентификаторЭДО", Истина);
	ОператорыЭДО = СервисНастроекЭДО.НайтиОператоровЭДОПоИдентификаторам(ИдентификаторыЭДО);
	
	Для Каждого УчетнаяЗапись Из УчетныеЗаписи Цикл
		УчетнаяЗапись.Вставить("ИдентификаторОператора");
		УчетнаяЗапись.Вставить("ПредставлениеОператора");
		ОператорЭДО = ОператорыЭДО.Найти(УчетнаяЗапись.ИдентификаторЭДО, "ИдентификаторЭДО");
		Если ОператорЭДО <> Неопределено Тогда
			ЗаполнитьЗначенияСвойств(УчетнаяЗапись, ОператорЭДО);
		КонецЕсли;
	КонецЦикла;
	
	Возврат УчетныеЗаписи;
	
КонецФункции

// Возвращаемое значение:
//  Структура:
//  * ЗаполнятьРеквизитыПоИНН - Булево
//
Функция ПараметрыПомощникаПодключенияЭДО() Экспорт
	
	ПараметрыПомощника = Новый Структура;
	ПараметрыПомощника.Вставить("ЗаполнятьРеквизитыПоИНН", Ложь);
	ОбменСКонтрагентамиПереопределяемый.ПриОпределенииПараметровПомощникаПодключенияЭДО(ПараметрыПомощника);
	Возврат ПараметрыПомощника;
	
КонецФункции

#Область ПереводАбонентовИзТакскомВХаб

// Параметры:
//  КлючиСинхронизации - См. СинхронизацияЭДОКлиентСервер.НовыеКлючиСинхронизации
Процедура ПеревестиУчетныеЗаписиТакскомВХаб(КлючиСинхронизации) Экспорт

	УстановитьПривилегированныйРежим(Истина);
	Если Не ДоступенПереводАбонентовИзТакскомВХаб() Тогда
		Возврат;
	КонецЕсли;
	
	УчетныеЗаписиПереведенныеИзТакском = СписокУчетныхЗаписейПереведенныхИзТакском();
	КлючиСинхронизацииЗаписейКПереводу = Новый Соответствие;
	СертификатыКлючейКПереводу = Новый Массив;
	Для Каждого КлючЗначениеКлючаСинхронизации Из КлючиСинхронизации Цикл
		КлючСинхронизации = КлючЗначениеКлючаСинхронизации.Значение;
		Если КлючСинхронизации.СпособОбмена = Перечисления.СпособыОбменаЭД.ЧерезОператораЭДОТакском
			И УчетныеЗаписиПереведенныеИзТакском.Найти(КлючСинхронизации.ИдентификаторУчетнойЗаписи) = Неопределено Тогда
			
			КлючиСинхронизацииЗаписейКПереводу.Вставить(КлючЗначениеКлючаСинхронизации.Ключ, КлючСинхронизации);
			СертификатыКлючейКПереводу.Добавить(КлючСинхронизации.ВыбранныйСертификат);
		КонецЕсли;
	КонецЦикла;
	
	СвойстваСертификатов = КриптографияБЭД.СвойстваСертификатов(СертификатыКлючейКПереводу);
	Для Каждого КлючЗначениеКлючаСинхронизацииКПереводу Из КлючиСинхронизацииЗаписейКПереводу Цикл
		КлючСинхронизации = КлючЗначениеКлючаСинхронизацииКПереводу.Значение;
		СвойстваСертификата = СвойстваСертификатов.Получить(КлючСинхронизации.ВыбранныйСертификат);	
		Если ЗначениеЗаполнено(СвойстваСертификата) Тогда
			СервисЭДО.ПеревестиУчетнуюЗаписьИзТакскомВХаб(КлючСинхронизации, 
				СвойстваСертификата.ДанныеСертификата);
		КонецЕсли;
	КонецЦикла;
	
	УстановитьПривилегированныйРежим(Ложь);
КонецПроцедуры

Процедура ИзменитьСпособОбменаВсехУчетныхЗаписейПереводимыхИзТакском() Экспорт
	УстановитьПривилегированныйРежим(Истина);
	
	ДатаПереводаАбонентовИзТакском = ДатаПереводаАбонентовИзТакском();
	Если ЗначениеЗаполнено(ДатаПереводаАбонентовИзТакском) 
		И ДатаПереводаАбонентовИзТакском < ТекущаяДатаСеанса() Тогда
		
		Запрос = Новый Запрос;
		Запрос.Текст = 
		"ВЫБРАТЬ
		|	УчетныеЗаписиЭДО.ИдентификаторЭДО КАК ИдентификаторЭДО
		|ИЗ
		|	РегистрСведений.УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
		|ГДЕ
		|	УчетныеЗаписиЭДО.СпособОбменаЭД = &СпособОбменаЭД";
		
		Запрос.УстановитьПараметр("СпособОбменаЭД", Перечисления.СпособыОбменаЭД.ЧерезОператораЭДОТакском);
		
		РезультатЗапроса = Запрос.Выполнить();
		Если РезультатЗапроса.Пустой() Тогда
			Возврат;
		КонецЕсли;
		
		ВыборкаУчетныеЗаписи = РезультатЗапроса.Выбрать();
		
		Пока ВыборкаУчетныеЗаписи.Следующий() Цикл
			ИзменитьСпособОбменаУчетнойЗаписиПереводимойИзТакском(ВыборкаУчетныеЗаписи.ИдентификаторЭДО);
		КонецЦикла;
	КонецЕсли;
	УстановитьПривилегированныйРежим(Ложь);
КонецПроцедуры

// Параметры:
//  ИдентификаторЭДО - Строка
Процедура ИзменитьСпособОбменаУчетнойЗаписиПереводимойИзТакском(ИдентификаторЭДО) Экспорт 
	
	УстановитьПривилегированныйРежим(Истина);
	
	Если УчетнаяЗаписьПереведенаИзТакском(ИдентификаторЭДО) Тогда
		Возврат;
	КонецЕсли;
	
	ПереводЗафиксирован = Ложь;
		
	НачатьТранзакцию();
	Попытка 
		
		ИзменитьСпособОбменаВПереводимойУчетнойЗаписиИзТакском(ИдентификаторЭДО);
		ИзменитьСпособОбменаОператораТакском();
		
		ДатыЗапросаДанныхОператораЭДО = СинхронизацияЭДО.ДатыЗапросаДанныхОператораЭДО(
			ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ИдентификаторЭДО), 
			СинхронизацияЭДО.ТипДанныхЗапросаОператораЭДОДокументы());
		ДатыЗапросаДанныхОператораЭДО = ДатыЗапросаДанныхОператораЭДО.Получить(ИдентификаторЭДО);
		
		Набор = РегистрыСведений.УчетныеЗаписиПереведенныеИзТакскомЭДО.СоздатьНаборЗаписей();
		Набор.Отбор.ИдентификаторЭДО.Установить(ИдентификаторЭДО);
		Запись = Набор.Добавить();
		Запись.ИдентификаторЭДО = ИдентификаторЭДО;
		
		Если ЗначениеЗаполнено(ДатыЗапросаДанныхОператораЭДО) Тогда
			Запись.ДатаПолучения = ДатыЗапросаДанныхОператораЭДО.ДатаПолучения;
			Запись.ДатаПолученияЭПД = ДатыЗапросаДанныхОператораЭДО.ДатаПолученияЭПД;
			Запись.ПроверочнаяДатаПолучения = ДатыЗапросаДанныхОператораЭДО.ПроверочнаяДатаПолучения;
		КонецЕсли;
		
		Набор.Записать();
		
		ПереводЗафиксирован = Истина;
		
		ЗафиксироватьТранзакцию();
	Исключение 
		ОтменитьТранзакцию();
				
		Информация = ИнформацияОбОшибке();
		
		ПодробныйТекстОшибкиПеревода = СтрШаблон(НСтр("ru = 'Не удалось выполнить изменение способа обмена учетной записи %1 по причине: %2'"),
			ИдентификаторЭДО, 
			ОбработкаОшибок.ПодробноеПредставлениеОшибки(Информация));
		
		ЭлектронноеВзаимодействие.ОбработатьОшибку(НСтр("ru = 'Изменение способа обмена учетной записи'"),
			ПодробныйТекстОшибкиПеревода);
		
	КонецПопытки;
	
	Если ПереводЗафиксирован Тогда
		ОтклонитьВсеНепринятыеПриглашенияПриПереводеИзТакском(ИдентификаторЭДО);
	КонецЕсли;
	
	УстановитьПривилегированныйРежим(Ложь);

КонецПроцедуры

// Параметры:
//  ИдентификаторЭДО - Строка
// 
// Возвращаемое значение:
//  Булево 
Функция УчетнаяЗаписьПереведенаИзТакском(ИдентификаторЭДО) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	УчетныеЗаписиПереведенныеИзТакском.ИдентификаторЭДО
		|ИЗ
		|	РегистрСведений.УчетныеЗаписиПереведенныеИзТакскомЭДО КАК УчетныеЗаписиПереведенныеИзТакском
		|ГДЕ
		|	УчетныеЗаписиПереведенныеИзТакском.ИдентификаторЭДО = &ИдентификаторЭДО";
	
	Запрос.УстановитьПараметр("ИдентификаторЭДО", ИдентификаторЭДО);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Возврат Не РезультатЗапроса.Пустой();
КонецФункции

// Возвращаемое значение:
//  Массив Из Строка
Функция СписокУчетныхЗаписейПереведенныхИзТакском() Экспорт 
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	УчетныеЗаписиПереведенныеИзТакском.ИдентификаторЭДО
		|ИЗ
		|	РегистрСведений.УчетныеЗаписиПереведенныеИзТакскомЭДО КАК УчетныеЗаписиПереведенныеИзТакском";
		
	РезультатЗапроса = Запрос.Выполнить();
	
	Результат = РезультатЗапроса.Выгрузить().ВыгрузитьКолонку("ИдентификаторЭДО");
	Возврат Результат;
КонецФункции

// Возвращаемое значение:
//  Булево 
Функция ТребуетсяВыполнениеДействийПоAPIТакском() Экспорт
	
	Если НаступилаДатаОтключенияAPIТакском() Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	УчетныеЗаписиПереведенныеИзТакском.ИдентификаторЭДО
		|ИЗ
		|	РегистрСведений.УчетныеЗаписиПереведенныеИзТакскомЭДО КАК УчетныеЗаписиПереведенныеИзТакском";

	РезультатЗапроса = Запрос.Выполнить();
	
	Возврат Не РезультатЗапроса.Пустой();
КонецФункции

// Возвращаемое значение:
//  Булево
Функция НаступилаДатаОтключенияAPIТакском() Экспорт
	ДатаОтключенияAPIТакском = ДатаОтключенияAPIТакском();
	Возврат ЗначениеЗаполнено(ДатаОтключенияAPIТакском) И ДатаОтключенияAPIТакском < ТекущаяДатаСеанса();
КонецФункции

// Возвращаемое значение:
//  Дата
Функция ДатаОтключенияAPIТакском() Экспорт
	ЗагрузитьДатыПереводаАбонентовИзТакском();
	
	УстановитьПривилегированныйРежим(Истина);
	Результат = Константы.ДатаОтключенияAPIТакскомЭДО.Получить();
	УстановитьПривилегированныйРежим(Ложь);
	Возврат Результат;
КонецФункции

// Возвращаемое значение:
//  Булево
Функция НаступилаДатаПереводаАбонентовИзТакском() Экспорт
	ДатаПереводаАбонентовИзТакском = ДатаПереводаАбонентовИзТакском();
	Возврат ЗначениеЗаполнено(ДатаПереводаАбонентовИзТакском) И ДатаПереводаАбонентовИзТакском < ТекущаяДатаСеанса();
КонецФункции

// Параметры:
//  КлючиСинхронизации - См. СинхронизацияЭДОКлиентСервер.НовыеКлючиСинхронизации
//  ПараметрыВыполнения - См. ЭлектронныеДокументыЭДОКлиентСервер.НовыеПараметрыВыполненияДействийПоЭДО
Процедура СкорректироватьСпособОбменаВКлючахСинхронизации(КлючиСинхронизации, ПараметрыВыполнения) Экспорт
	Если Не ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(ПараметрыВыполнения, "ВыполнитьТолькоПоТакском", Ложь) Тогда
		Возврат;
	КонецЕсли;
	
	Если Не ТребуетсяВыполнениеДействийПоAPIТакском() Тогда
		Возврат;
	КонецЕсли;
	
	УчетныеЗаписиПереведенныеИзТакском = СписокУчетныхЗаписейПереведенныхИзТакском();
	Для каждого КлючЗначение Из КлючиСинхронизации Цикл
		Если УчетныеЗаписиПереведенныеИзТакском.Найти(КлючЗначение.Ключ) <> Неопределено Тогда
			КлючЗначение.Значение.СпособОбмена = Перечисления.СпособыОбменаЭД.ЧерезОператораЭДОТакском;
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

// Параметры:
//  КлючиСинхронизации - См. СинхронизацияЭДОКлиентСервер.НовыеКлючиСинхронизации
Процедура УдалитьПереведенныеУчетныеЗаписиТакскомаИзКлючейСинхронизации(КлючиСинхронизации) Экспорт
	Если Не ЗначениеЗаполнено(КлючиСинхронизации) Тогда
		Возврат;
	КонецЕсли;

	УчетныеЗаписиПереведенныеИзТакском = СписокУчетныхЗаписейПереведенныхИзТакском();
	Если ЗначениеЗаполнено(УчетныеЗаписиПереведенныеИзТакском) Тогда
		ИдентификаторыЭДОКУдалениюИзКлючей = Новый Массив;
		
		Для каждого КлючЗначение Из КлючиСинхронизации Цикл
			ИдентификаторЭДО = КлючЗначение.Ключ;
			СвойстваКлюча = КлючЗначение.Значение;
			
			Если СвойстваКлюча.СпособОбмена = Перечисления.СпособыОбменаЭД.ЧерезОператораЭДОТакском 
				И УчетныеЗаписиПереведенныеИзТакском.Найти(ИдентификаторЭДО) <> Неопределено Тогда
					
				ИдентификаторыЭДОКУдалениюИзКлючей.Добавить(ИдентификаторЭДО);
			КонецЕсли;
		КонецЦикла;
		
		Для каждого ИдентификаторЭДОКУдалению Из ИдентификаторыЭДОКУдалениюИзКлючей Цикл
			КлючиСинхронизации.Удалить(ИдентификаторЭДОКУдалению);
		КонецЦикла;
	КонецЕсли;
КонецПроцедуры

#Если Не ВнешнееСоединение Тогда
// Параметры:
//  Форма - ФормаКлиентскогоПриложения
Процедура НастроитьГруппуПредупреждениеОЗавершенииДокументовТакском(Форма) Экспорт
	ПоказыватьСписокДокументовТакскомТребующихЗавершения = ПоказыватьСписокДокументовТакскомТребующихЗавершения();
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
		Форма.Элементы, 
		"ГруппаПредупреждениеОЗавершенииДокументовТакском", 
		"Видимость",
		ПоказыватьСписокДокументовТакскомТребующихЗавершения);

	Если ПоказыватьСписокДокументовТакскомТребующихЗавершения Тогда
		ШаблонНадписиГруппы = НСтр("ru = 'В связи с переходом оператора Такском на платформу 1С:Хаб необходимо до %1 завершить документооборот для некоторых электронных документов. <a href = ""%2"">Количество документов к завершению %3</a>.'");
		ТекстСообщения = СтрШаблон(ШаблонНадписиГруппы,
			Формат(ДатаОтключенияAPIТакском(), "ДЛФ=DD"),
			"ОткрытьФормуНезавершенныхДокументовТакском",
			КоличествоНезавершенныхДокументовТакскомПоСтаромуAPI());
		ФорматированныйЗаголовок = СтроковыеФункции.ФорматированнаяСтрока(ТекстСообщения);
		
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
			Форма.Элементы, 
			"НадписьПредупреждениеОЗавершенииДокументовТакском", 
			"Заголовок",
			ФорматированныйЗаголовок);
		
	КонецЕсли;
КонецПроцедуры

#КонецЕсли

// Возвращаемое значение:
//  Булево
Функция ПоказыватьСписокДокументовТакскомТребующихЗавершения() Экспорт
 
	ДатаОтключенияAPIТакском = ДатаОтключенияAPIТакском();
	Если Не ЗначениеЗаполнено(ДатаОтключенияAPIТакском) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ДатаНачалаПоказаУведомления = ДобавитьМесяц(ДатаОтключенияAPIТакском, -2);
	Если ДатаНачалаПоказаУведомления > ТекущаяДатаСеанса() Тогда
		Возврат Ложь;
	КонецЕсли;
		
	Если Не ЗначениеЗаполнено(СписокУчетныхЗаписейПереведенныхИзТакском()) Тогда
		Возврат Ложь;
	КонецЕсли; 
	
	КоличествоНезавершенныхДокументовТакском = КоличествоНезавершенныхДокументовТакскомПоСтаромуAPI();
	Если КоличествоНезавершенныхДокументовТакском = 0 Тогда
		Возврат Ложь;
	КонецЕсли;

	Возврат Истина;
КонецФункции

// Параметры:
//  ИдентификаторЭДО - Строка
//  ОшибкаОператора - Неопределено,
//					- См. СервисЭДО.НоваяОшибкаОператора
Процедура ДобавитьОшибкуПереводаИзТакскомВХаб(ИдентификаторЭДО, ОшибкаОператора) Экспорт 
	
	УстановитьПривилегированныйРежим(Истина);
	Если Не ЗначениеЗаполнено(ИдентификаторЭДО) Тогда
		Возврат;
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ОшибкаОператора) Тогда
		Возврат;
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ОшибкаОператора.Текст) Тогда
		Возврат;
	КонецЕсли;
	
	ИдентификаторОшибки = Строка(Новый УникальныйИдентификатор);
	ДатаОшибки = ТекущаяДатаСеанса();
	
	ОшибкиПереводаАбонентовВХАБ = РегистрыСведений.ОшибкиПереводаАбонентовВХАБЭДО.СоздатьНаборЗаписей();
	ОшибкиПереводаАбонентовВХАБ.Отбор.ИдентификаторЭДО.Установить(ИдентификаторЭДО);
	ОшибкиПереводаАбонентовВХАБ.Отбор.ИдентификаторОшибки.Установить(ИдентификаторОшибки);
	ОшибкиПереводаАбонентовВХАБ.Отбор.Дата.Установить(ДатаОшибки);
	
	НоваяОшибки = ОшибкиПереводаАбонентовВХАБ.Добавить();
	НоваяОшибки.ИдентификаторЭДО = ИдентификаторЭДО;
	НоваяОшибки.ИдентификаторОшибки = ИдентификаторОшибки;
	НоваяОшибки.ТекстОшибки = ОшибкаОператора.Текст;
	НоваяОшибки.Дата = ДатаОшибки;
	
	ОшибкиПереводаАбонентовВХАБ.Записать();
	
	УстановитьПривилегированныйРежим(Ложь);
КонецПроцедуры

#КонецОбласти

// Устанавливает признак создавать контрагентов автоматически
//
Процедура УстановитьПризнакАвтосозданияКонтрагентов() Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	Если Константы.СоздаватьКонтрагентовАвтоматическиБЭД.Получить() Тогда
		Возврат;
	КонецЕсли;
	
	Константы.СоздаватьКонтрагентовАвтоматическиБЭД.Установить(Истина);
	
КонецПроцедуры

// Параметры:
//  ИдентификаторЭДО - Строка
Процедура УстановитьПризнакПереходаВЦентрЭДО(ИдентификаторЭДО) Экспорт
	
	РегистрыСведений.УчетныеЗаписиЭДО.УстановитьПризнакПереходаВЦентрЭДО(ИдентификаторЭДО);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

#Область АвторизацияВСервисеЭДО

// Параметры:
//  ИдентификаторыЭДО - Массив из Строка
// 
// Возвращаемое значение:
//  Соответствие из КлючИЗначение:
//  * Ключ - Строка
//  * Значение - Строка
Функция ПредставленияУчетныхЗаписейЭДО(ИдентификаторыЭДО)
	
	Результат = Новый Соответствие;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	УчетныеЗаписиЭДО.ИдентификаторЭДО КАК ИдентификаторЭДО,
		|	УчетныеЗаписиЭДО.НаименованиеУчетнойЗаписи КАК НаименованиеУчетнойЗаписи
		|ИЗ
		|	РегистрСведений.УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
		|ГДЕ
		|	УчетныеЗаписиЭДО.ИдентификаторЭДО В (&ИдентификаторыЭДО)";
	
	Запрос.УстановитьПараметр("ИдентификаторыЭДО", ИдентификаторыЭДО);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		Результат.Вставить(Выборка.ИдентификаторЭДО, Выборка.НаименованиеУчетнойЗаписи);
	КонецЦикла;
	
	Возврат Результат
	
КонецФункции

#КонецОбласти

// Параметры:
//  ИмяВременнойТаблицы - Строка
//  ВыбираемыеПоля - Строка
//  ПоляУсловия - Строка
// 
// Возвращаемое значение:
//  Строка
Функция ШаблонЗапросаУчетныхЗаписей(ИмяВременнойТаблицы, ВыбираемыеПоля, ПоляУсловия = "") 
	
	ТекстЗапроса = "ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	&ВыбираемыеПоля
	|ПОМЕСТИТЬ ИмяВременнойТаблицы
	|ИЗ
	|	РегистрСведений.УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
	|ГДЕ
	|	&ПоляУсловия";
	
	Если ИмяВременнойТаблицы = "" Тогда
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ПОМЕСТИТЬ ИмяВременнойТаблицы", "");
	Иначе
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ИмяВременнойТаблицы", ИмяВременнойТаблицы);
	КонецЕсли;
	
	Возврат ОбщегоНазначенияБЭД.ТекстЗапросаИзШаблона(ТекстЗапроса, "УчетныеЗаписиЭДО", ВыбираемыеПоля, ПоляУсловия);
	
КонецФункции

// Параметры:
//  Организация - ОпределяемыйТип.Организация
//  Свойства - Строка
// 
// Возвращаемое значение:
//  Массив Из Структура
Функция ЗначенияСвойствУчетныхЗаписейОрганизацииСервисаЭДО(Организация, Свойства)
	
	ТекстЗапроса =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ОрганизацииПоИНН.Ссылка КАК Ссылка
		|ПОМЕСТИТЬ ВТ_Организации
		|ИЗ
		|	ИмяТаблицыОрганизации КАК Организации
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ИмяТаблицыОрганизации КАК ОрганизацииПоИНН
		|		ПО Организации.ИмяРеквизитаИНН = ОрганизацииПоИНН.ИмяРеквизитаИНН
		|			И &УсловиеПоРеквизитуЮрФизЛицо
		|ГДЕ
		|	Организации.Ссылка = &Организация
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	&Свойства,
		|	УчетныеЗаписиЭДО.НаименованиеУчетнойЗаписи КАК Представление
		|ИЗ
		|	РегистрСведений.УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_Организации КАК ВТ_Организации
		|		ПО УчетныеЗаписиЭДО.Организация = ВТ_Организации.Ссылка
		|ГДЕ
		|	УчетныеЗаписиЭДО.СпособОбменаЭД В (&СпособыОбменаЧерезОператора)";
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&Свойства", Свойства);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ИмяТаблицыОрганизации", ОбщегоНазначения.ИмяТаблицыПоСсылке(Организация));
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ИмяРеквизитаИНН",
		ИнтеграцияЭДО.ИмяНаличиеОбъектаРеквизитаВПрикладномРешении("ИННОрганизации"));
	
	ИмяРеквизитаЮрФизЛицо = ИнтеграцияЭДО.ИмяНаличиеОбъектаРеквизитаВПрикладномРешении("ЮрФизЛицоОрганизации");
	Если ЗначениеЗаполнено(ИмяРеквизитаЮрФизЛицо) Тогда
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&УсловиеПоРеквизитуЮрФизЛицо", "Организации.ИмяРеквизитаЮрФизЛицо = ОрганизацииПоИНН.ИмяРеквизитаЮрФизЛицо");
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ИмяРеквизитаЮрФизЛицо", ИмяРеквизитаЮрФизЛицо);
	Иначе
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "И &УсловиеПоРеквизитуЮрФизЛицо", "");
	КонецЕсли;
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("Организация", Организация);
	Запрос.УстановитьПараметр("СпособыОбменаЧерезОператора", СинхронизацияЭДО.СпособыОбменаЧерезОператора());
	
	МассивЗначенийСвойств = Новый Массив;
	
	РезультатЗапроса = Запрос.Выполнить();
	Если Не РезультатЗапроса.Пустой() Тогда
		Выборка = РезультатЗапроса.Выбрать();
		Пока Выборка.Следующий() Цикл
			ЗначенияСвойств = Новый Структура(Свойства);
			ЗаполнитьЗначенияСвойств(ЗначенияСвойств, Выборка);
			МассивЗначенийСвойств.Добавить(ЗначенияСвойств);
		КонецЦикла;
	КонецЕсли;
	
	Если ОбщегоНазначения.ПодсистемаСуществует(
		"ЭлектронноеВзаимодействие.ЭлектронноеАктированиеЕИС") Тогда
		МодульЭлектронноеАктированиеЕИС = ОбщегоНазначения.ОбщийМодуль("ЭлектронноеАктированиеЕИС");
		Если МодульЭлектронноеАктированиеЕИС.ИспользоватьЭлектронноеАктированиеВЕИС() Тогда
			МодульЭлектронноеАктированиеЕИС.ДополнитьЗначенияСвойствУчетныхЗаписейОрганизацииСервисаЭДО(
				Организация,
				Свойства,
				МассивЗначенийСвойств);
		КонецЕсли;
	КонецЕсли;
		
	Возврат МассивЗначенийСвойств;
	
КонецФункции

// Параметры:
//  ИдентификаторУчетнойЗаписи - Строка
//
// Возвращаемое значение:
//  Массив из ХранилищеЗначения
Функция ДанныеНеотозванныхСертификатовУчетнойЗаписи(ИдентификаторУчетнойЗаписи)
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	СертификатыУчетныхЗаписейЭДО.Сертификат КАК Сертификат
		|ИЗ
		|	РегистрСведений.СертификатыУчетныхЗаписейЭДО КАК СертификатыУчетныхЗаписейЭДО
		|ГДЕ
		|	СертификатыУчетныхЗаписейЭДО.ИдентификаторЭДО = &ИдентификаторЭДО
		|	И РАЗНОСТЬДАТ(&ТекущаяДата, СертификатыУчетныхЗаписейЭДО.ДействителенДо, МИНУТА) > 0";
	Запрос.УстановитьПараметр("ИдентификаторЭДО", ИдентификаторУчетнойЗаписи);
	Запрос.УстановитьПараметр("ТекущаяДата", ТекущаяДатаСеанса());
	ДействительныеСертификаты = Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Сертификат");
	Возврат ИнтеграцияБСПБЭД.ДанныеНеотозванныхСертификатов(ДействительныеСертификаты);
	
КонецФункции

// Параметры:
//  ИдентификаторУчетнойЗаписи - Строка
//
// Возвращаемое значение:
//  Число
Функция КоличествоОтозванныхСертификатовУчетнойЗаписи(ИдентификаторУчетнойЗаписи)
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	СертификатыУчетныхЗаписейЭДО.Сертификат КАК Сертификат
		|ИЗ
		|	РегистрСведений.СертификатыУчетныхЗаписейЭДО КАК СертификатыУчетныхЗаписейЭДО
		|ГДЕ
		|	СертификатыУчетныхЗаписейЭДО.ИдентификаторЭДО = &ИдентификаторЭДО";
	Запрос.УстановитьПараметр("ИдентификаторЭДО", ИдентификаторУчетнойЗаписи);
	СертификатыУчетнойЗаписи = Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Сертификат");
	Возврат ИнтеграцияБСПБЭД.КоличествоОтозванныхСертификатов(СертификатыУчетнойЗаписи);
	
КонецФункции

// Параметры:
//  ИдентификаторУчетнойЗаписи - Строка
//
// Возвращаемое значение:
//  Число
Функция КоличествоПросроченныхСертификатовУчетнойЗаписи(ИдентификаторУчетнойЗаписи)
	
	Результат = 0;
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	КОЛИЧЕСТВО(СертификатыУчетныхЗаписейЭДО.Сертификат) КАК КоличествоСертификатов
		|ИЗ
		|	РегистрСведений.СертификатыУчетныхЗаписейЭДО КАК СертификатыУчетныхЗаписейЭДО
		|ГДЕ
		|	СертификатыУчетныхЗаписейЭДО.ИдентификаторЭДО = &ИдентификаторЭДО
		|	И РАЗНОСТЬДАТ(&ТекущаяДата, СертификатыУчетныхЗаписейЭДО.ДействителенДо, МИНУТА) < 0";
	Запрос.УстановитьПараметр("ИдентификаторЭДО", ИдентификаторУчетнойЗаписи);
	Запрос.УстановитьПараметр("ТекущаяДата", ТекущаяДатаСеанса());
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		Результат = Выборка.КоличествоСертификатов;
	КонецЕсли;
	Возврат Результат;
	
КонецФункции

// Параметры:
// ИдентификаторУчетнойЗаписи - Строка
Процедура ПроверкаНаОтзывДействительныхСертификатов(ИдентификаторУчетнойЗаписи)
	
	МенеджерКриптографии = ЭлектроннаяПодпись.МенеджерКриптографии("", Ложь);
	ДанныеДействительныхСертификатов = ДанныеНеотозванныхСертификатовУчетнойЗаписи(ИдентификаторУчетнойЗаписи);
	Для Каждого ДанныеСертификата Из ДанныеДействительныхСертификатов Цикл
		ОписаниеОшибки = "";
		ЭлектроннаяПодпись.ПроверитьСертификат(МенеджерКриптографии, ДанныеСертификата.Получить(),
			ОписаниеОшибки);
	КонецЦикла;
	
КонецПроцедуры

// Параметры:
//  ИдентификаторЭДО - Строка
//  СпособОбмена - ПеречислениеСсылка.СпособыОбменаЭД
//  ИспользоватьЦентрЭДО - Булево
Процедура ПриУдаленииУчетнойЗаписи(ИдентификаторЭДО, СпособОбмена, ИспользоватьЦентрЭДО)
	
	НаборЗаписей = РегистрыСведений.СертификатыУчетныхЗаписейЭДО.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.ИдентификаторЭДО.Установить(ИдентификаторЭДО);
	НаборЗаписей.Записать();
	
	Если СпособОбмена = Перечисления.СпособыОбменаЭД.ЧерезFTP Тогда
		УстановитьПривилегированныйРежим(Истина);
		ОбщегоНазначения.УдалитьДанныеИзБезопасногоХранилища(ИдентификаторЭДО);
		УстановитьПривилегированныйРежим(Ложь);
	КонецЕсли;
	
	НастройкиЭДО.ПриУдаленииУчетнойЗаписи(ИдентификаторЭДО);
	ПриглашенияЭДО.ПриУдаленииУчетнойЗаписи(ИдентификаторЭДО);
	ОповещенияОСобытияхЭДО.ПриУдаленииУчетнойЗаписи(ИдентификаторЭДО);
	АвтоматическоеПолучениеЭДО.ПриУдаленииУчетнойЗаписи(ИдентификаторЭДО, СпособОбмена, ИспользоватьЦентрЭДО);
	
	Если ИспользоватьЦентрЭДО Тогда
		МодульУчетныеЗаписиЭДОИнтеграцияЦентраЭДО = ОбщегоНазначения.ОбщийМодуль("УчетныеЗаписиЭДОИнтеграцияЦентраЭДО");
		МодульУчетныеЗаписиЭДОИнтеграцияЦентраЭДО.ПриУдаленииУчетнойЗаписи(ИдентификаторЭДО);
	Иначе
		УчетныеЗаписиЭДОСлужебный.ПриУдаленииУчетнойЗаписи(ИдентификаторЭДО);
	КонецЕсли;
	
	Если ОбщегоНазначения.ПодсистемаСуществует("ЭлектронноеВзаимодействие.ЦентрЭДО") Тогда
		МодульУчетныеЗаписиЭДОЦентрЭДО = ОбщегоНазначения.ОбщийМодуль("УчетныеЗаписиЭДОЦентрЭДО");
		МодульУчетныеЗаписиЭДОЦентрЭДО.ПриУдаленииУчетнойЗаписи(ИдентификаторЭДО);
	КонецЕсли;
	
КонецПроцедуры

#Область ПереводАбонентовИзТакскомВХаб

// Возвращаемое значение:
//  Булево
Функция ДоступенПереводАбонентовИзТакскомВХаб()
	Возврат Ложь; // проектное поведение, перевод авторизацией выполняется только при наличии патча
КонецФункции

// Возвращаемое значение:
//  Число
Функция КоличествоНезавершенныхДокументовТакскомПоСтаромуAPI()
	Результат = 0;
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ЧерезОператораЭДОТакском", Перечисления.СпособыОбменаЭД.ЧерезОператораЭДОТакском);
	Запрос.УстановитьПараметр("СостоянияЗавершенногоЭДО", РегламентыЭДО.СостоянияЗавершенногоЭДО());
	Запрос.Текст =
	"ВЫБРАТЬ
	|	КОЛИЧЕСТВО(СостоянияДокументовЭДО.ЭлектронныйДокумент) КАК ЭлектронныйДокумент
	|ПОМЕСТИТЬ ВТ_ВсеДокументы
	|ИЗ
	|	РегистрСведений.СостоянияДокументовЭДО КАК СостоянияДокументовЭДО
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ЭлектронныйДокументВходящийЭДО КАК ЭлектронныйДокументВходящийЭДО
	|		ПО СостоянияДокументовЭДО.ЭлектронныйДокумент = ЭлектронныйДокументВходящийЭДО.Ссылка
	|		И НЕ СостоянияДокументовЭДО.Состояние В (&СостоянияЗавершенногоЭДО)
	|		И ЭлектронныйДокументВходящийЭДО.СпособОбмена = &ЧерезОператораЭДОТакском
	|		И ЭлектронныйДокументВходящийЭДО.ПометкаУдаления = ЛОЖЬ
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	КОЛИЧЕСТВО(СостоянияДокументовЭДО.ЭлектронныйДокумент)
	|ИЗ
	|	РегистрСведений.СостоянияДокументовЭДО КАК СостоянияДокументовЭДО
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ЭлектронныйДокументИсходящийЭДО КАК ЭлектронныйДокументИсходящийЭДО
	|		ПО СостоянияДокументовЭДО.ЭлектронныйДокумент = ЭлектронныйДокументИсходящийЭДО.Ссылка
	|		И НЕ СостоянияДокументовЭДО.Состояние В (&СостоянияЗавершенногоЭДО)
	|		И ЭлектронныйДокументИсходящийЭДО.СпособОбмена = &ЧерезОператораЭДОТакском
	|		И ЭлектронныйДокументИсходящийЭДО.ПометкаУдаления = ЛОЖЬ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	СУММА(ВТ_ВсеДокументы.ЭлектронныйДокумент) КАК ЭлектронныйДокумент
	|ИЗ
	|	ВТ_ВсеДокументы КАК ВТ_ВсеДокументы";
	
	РезультатЗапроса = Запрос.Выполнить();
	Выборка = РезультатЗапроса.Выбрать();
	Если Выборка.Следующий() Тогда
		Результат = Выборка.ЭлектронныйДокумент;
	КонецЕсли;
	Возврат Результат;

КонецФункции

// Параметры:
//  ИдентификаторЭДО - Строка 
Процедура ИзменитьСпособОбменаВПереводимойУчетнойЗаписиИзТакском(ИдентификаторЭДО)
	Поля = Новый Структура;
	Поля.Вставить("ИдентификаторЭДО", ИдентификаторЭДО);
	ОбщегоНазначенияБЭД.УстановитьУправляемуюБлокировку("РегистрСведений.УчетныеЗаписиЭДО", Поля);
	
	НаборЗаписей = РегистрыСведений.УчетныеЗаписиЭДО.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.ИдентификаторЭДО.Установить(ИдентификаторЭДО);
	НаборЗаписей.Прочитать();
	НаборЗаписей.Отбор.ИдентификаторЭДО.Установить(ИдентификаторЭДО);
	Для каждого СтрокаНабора Из НаборЗаписей Цикл 
		Если СтрокаНабора.СпособОбменаЭД = Перечисления.СпособыОбменаЭД.ЧерезОператораЭДОТакском Тогда
			СтрокаНабора.СпособОбменаЭД = Перечисления.СпособыОбменаЭД.ЧерезСервис1СЭДО;
			
			Если ВРег(СтрокаНабора.КодНалоговогоОргана) = "УКАЖ" Тогда
				СтрокаНабора.КодНалоговогоОргана = "";
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	НаборЗаписей.Записать();
КонецПроцедуры

Процедура ИзменитьСпособОбменаОператораТакском()
	ИдентификаторОператораТакском = "2AL";
	Поля = Новый Структура;
	Поля.Вставить("ИдентификаторОператора", ИдентификаторОператораТакском);
	ОбщегоНазначенияБЭД.УстановитьУправляемуюБлокировку("РегистрСведений.ОператорыЭДО", Поля);
	
	НаборЗаписей = РегистрыСведений.ОператорыЭДО.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.ИдентификаторОператора.Установить(ИдентификаторОператораТакском);
	НаборЗаписей.Прочитать();
	
	СпособИзменен = Ложь;
	НаборЗаписей.Отбор.ИдентификаторОператора.Установить(ИдентификаторОператораТакском);
	Для каждого СтрокаНабора Из НаборЗаписей Цикл 
		Если СтрокаНабора.СпособОбменаЭД = Перечисления.СпособыОбменаЭД.ЧерезОператораЭДОТакском Тогда
			СтрокаНабора.СпособОбменаЭД = Перечисления.СпособыОбменаЭД.ЧерезСервис1СЭДО; 
			СпособИзменен = Истина;
		КонецЕсли;
	КонецЦикла;
	
	Если СпособИзменен Тогда
		НаборЗаписей.Записать();
	КонецЕсли;
	
КонецПроцедуры

// Параметры:
//  ИдентификаторЭДО - Строка 
Процедура ОтклонитьВсеНепринятыеПриглашенияПриПереводеИзТакском(ИдентификаторЭДО) 
	Запросы = Новый Массив;
	
	Отбор = НовыйОтборУчетныхЗаписей();
	Отбор.УчетныеЗаписи = "&ИдентификаторОрганизации";
	ЗапросУчетныхЗаписей = ЗапросУчетныхЗаписей("УчетныеЗаписиЭДО", Отбор);
	Запросы.Добавить(ЗапросУчетныхЗаписей);
	
	ОтборПриглашений = ПриглашенияЭДО.НовыйОтборПриглашений();
	ОтборПриглашений.ИдентификаторОрганизации = "&ИдентификаторОрганизации";
	ОтборПриглашений.Статус = "&СтатусыНепринятыхПриглашений";
	ЗапросПриглашений = ПриглашенияЭДО.ЗапросПриглашений("Приглашения", ОтборПриглашений);
	Запросы.Добавить(ЗапросПриглашений);
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ
	|	Приглашения.КлючПриглашения КАК КлючПриглашения,
	|	Приглашения.ИдентификаторОрганизации КАК ИдентификаторОрганизации,
	|	УчетныеЗаписиЭДО.Организация КАК Организация
	|ИЗ
	|	Приглашения КАК Приглашения
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ УчетныеЗаписиЭДО КАК УчетныеЗаписиЭДО
	|		ПО Приглашения.ИдентификаторОрганизации = УчетныеЗаписиЭДО.ИдентификаторЭДО";

	ИтоговыйЗапрос = ОбщегоНазначенияБЭД.СоединитьЗапросы(Запрос, Запросы);
	ИтоговыйЗапрос.УстановитьПараметр("ИдентификаторОрганизации", ИдентификаторЭДО);
	
	СтатусыНепринятыхПриглашений = Новый Массив;
	СтатусыНепринятыхПриглашений.Добавить(Перечисления.СтатусыПриглашений.ОжидаемСогласия);
	СтатусыНепринятыхПриглашений.Добавить(Перечисления.СтатусыПриглашений.ТребуетсяСогласие);
	ИтоговыйЗапрос.УстановитьПараметр("СтатусыНепринятыхПриглашений", СтатусыНепринятыхПриглашений);	
	
	РезультатЗапроса = ИтоговыйЗапрос.Выполнить();
	Если РезультатЗапроса.Пустой() Тогда
		Возврат;
	КонецЕсли;
	
	СведенияОтклоненныхПриглашений = Новый Массив;
	
	Выборка = РезультатЗапроса.Выбрать();
	Пока Выборка.Следующий() Цикл
		
		НачатьТранзакцию();
		Попытка
			Поля = Новый Структура;
			Поля.Вставить("Ключ", Выборка.КлючПриглашения);
			Поля.Вставить("ИдентификаторОрганизации", Выборка.ИдентификаторОрганизации);
			ОбщегоНазначенияБЭД.УстановитьУправляемуюБлокировку("РегистрСведений.ПриглашенияКОбменуЭлектроннымиДокументами", Поля);
			
			НаборЗаписей = РегистрыСведений.ПриглашенияКОбменуЭлектроннымиДокументами.СоздатьНаборЗаписей();
			НаборЗаписей.Отбор.ИдентификаторОрганизации.Установить(Выборка.ИдентификаторОрганизации);
			НаборЗаписей.Отбор.Ключ.Установить(Выборка.КлючПриглашения);
			НаборЗаписей.Прочитать();
			
			НаборЗаписей.Отбор.ИдентификаторОрганизации.Установить(Выборка.ИдентификаторОрганизации);
			НаборЗаписей.Отбор.Ключ.Установить(Выборка.КлючПриглашения);
			
			СведенияОПриглашении = ПриглашенияЭДОСлужебный.СведенияОЗаписанномПриглашении();
			СведенияОПриглашении.Организация = Выборка.Организация;
			
			Для каждого СтрокаНабора Из НаборЗаписей Цикл 
				СтрокаНабора.Статус = Перечисления.СтатусыПриглашений.Отклонено;
				СтрокаНабора.ПричинаОтказа = НСтр("ru = 'Перевод в 1С:ХАБ'");
				
				ЗаполнитьЗначенияСвойств(СведенияОПриглашении, СтрокаНабора);
	
			КонецЦикла;
			
			НаборЗаписей.Записать();
			
			СведенияОтклоненныхПриглашений.Добавить(СведенияОПриглашении);
		
			ЗафиксироватьТранзакцию();
		Исключение
			ОтменитьТранзакцию();
			
			Информация = ИнформацияОбОшибке();
			
			ПодробныйТекстОшибкиПеревода = СтрШаблон(НСтр("ru = 'Не удалось отклонить приглашение при переводе учетной записи %1 по причине: %2'"),
			ИдентификаторЭДО, 
			ОбработкаОшибок.ПодробноеПредставлениеОшибки(Информация));
			
			ЭлектронноеВзаимодействие.ОбработатьОшибку(НСтр("ru = 'Отклонение приглашений'"),
			ПодробныйТекстОшибкиПеревода);
			
		КонецПопытки;
	КонецЦикла;
	
	КонтекстДиагностики = ОбработкаНеисправностейБЭД.НовыйКонтекстДиагностики();
	ПричинаОстановки = Перечисления.ПричиныОстановкиЭДО.ОтклонениеПриглашения;
	Для каждого СведенияПриглашения Из СведенияОтклоненныхПриглашений Цикл	
		ЭлектронныеДокументыЭДОСлужебный.ОстановитьДокументыПослеИзмененияПриглашения(
			СведенияПриглашения.ИдентификаторОрганизации,
			СведенияПриглашения.ИдентификаторКонтрагента, 
			ПричинаОстановки, 
			КонтекстДиагностики,
			Истина);
		
	КонецЦикла;
КонецПроцедуры

Процедура ЗагрузитьДатыПереводаАбонентовИзТакском()
	Если Не ТребуетсяСинхронизацияДатПереводаИзСервисаНастроек() Тогда 
		Возврат;
	КонецЕсли;
	
	ПолныйАдрес = СервисНастроекЭДО.АдресОблачногоХранилищаНастроек() + "/operators/2AL/APIDeprecationDate.json";
	РезультатПолученияФайла = ПолучениеФайловИзИнтернета.СкачатьФайлВоВременноеХранилище(ПолныйАдрес); 
	Если РезультатПолученияФайла.Статус Тогда
		УстановитьПривилегированныйРежим(Истина);
		
		ДанныеФайла = ПолучитьИзВременногоХранилища(РезультатПолученияФайла.Путь);
		ДатыИзСервиса = ОбщегоНазначенияБЭД.JSONЗначение(ДанныеФайла, Истина);
		
		Если ЗначениеЗаполнено(ДатыИзСервиса["ДатаПереводаАбонентовИзТакском"]) Тогда
			Константы.ДатаПереводаАбонентовИзТакскомЭДО.Установить(ПрочитатьДатуJSON(ДатыИзСервиса["ДатаПереводаАбонентовИзТакском"], ФорматДатыJSON.ISO));
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ДатыИзСервиса["ДатаОтключенияAPIТакском"]) Тогда
			Константы.ДатаОтключенияAPIТакскомЭДО.Установить(ПрочитатьДатуJSON(ДатыИзСервиса["ДатаОтключенияAPIТакском"], ФорматДатыJSON.ISO));
		КонецЕсли;
		
		СервисНастроекЭДО.ИзменитьСостояниеСинхронизации(Перечисления.ТипыСинхронизацииСервисаНастроекЭДО.СинхронизацияДатПереводаАбонентовИзТакскомВХАБ, 
			НачалоДня(ТекущаяДатаСеанса()));
		УстановитьПривилегированныйРежим(Ложь);
	КонецЕсли;
КонецПроцедуры 

// Возвращаемое значение:
//  Булево
Функция ТребуетсяСинхронизацияДатПереводаИзСервисаНастроек()
	
	ДатаСинхронизацииДатПереводаАбонентовИзТакскомВХАБ = СинхронизацияЭДО.ДатаПоследнегоИзмененияДанныхСервисаНастроек(
		Перечисления.ТипыСинхронизацииСервисаНастроекЭДО.СинхронизацияДатПереводаАбонентовИзТакскомВХАБ);
	
	Возврат НачалоДня(ДатаСинхронизацииДатПереводаАбонентовИзТакскомВХАБ) < НачалоДня(ТекущаяДатаСеанса()) 
		Или Не ЗначениеЗаполнено(ДатаСинхронизацииДатПереводаАбонентовИзТакскомВХАБ);
		
КонецФункции

// Возвращаемое значение:
//  Дата
Функция ДатаПереводаАбонентовИзТакском()
	ЗагрузитьДатыПереводаАбонентовИзТакском();
	
	УстановитьПривилегированныйРежим(Истина);
	Результат = Константы.ДатаПереводаАбонентовИзТакскомЭДО.Получить();
	УстановитьПривилегированныйРежим(Ложь);
	Возврат Результат;
КонецФункции

#КонецОбласти

// См. СтандартныеПодсистемыСервер.ПриОпределенииМетодовРазрешенныхДляВызоваКакПроизвольныйКод
Процедура ПриОпределенииМетодовРазрешенныхДляВызоваКакПроизвольныйКод(Методы) Экспорт
	
	Методы.Вставить("АвторизоватьсяВСервисеЭДО", Истина);
	Методы.Вставить("ДанныеПоИдентификаторамДляРегистрацииВЭДО", Истина);
	Методы.Вставить("УдалитьУчетнуюЗапись", Истина);
	
КонецПроцедуры

// Получает должность по сертификату учетной записи
// 
// Параметры:
//  ИдентификаторЭДО - Строка
//  Сертификат - СправочникСсылка.СертификатыКлючейЭлектроннойПодписиИШифрования
//
// Возвращаемое значение:
//  Строка - Должность
Функция ДолжностьПоСертификатуУчетнойЗаписи(ИдентификаторЭДО, Сертификат) Экспорт
	
	ОтборСертификата = НовыйОтборСертификатовУчетныхЗаписей();
	ОтборСертификата.УчетныеЗаписи = "&ИдентификаторыУчетныхЗаписейЭДО";
	ОтборСертификата.Сертификат = "&Сертификат";
	ЗапросДолжностиВУчетнойЗаписи = ЗапросСертификатовУчетныхЗаписей("", ОтборСертификата);
	
	ИтоговыйЗапрос = Новый Запрос;
	ИтоговыйЗапрос.Текст = СтрЗаменить(ЗапросДолжностиВУчетнойЗаписи.Текст, "ПОМЕСТИТЬ", "");
	ИтоговыйЗапрос.УстановитьПараметр("ИдентификаторыУчетныхЗаписейЭДО", ИдентификаторЭДО);
	ИтоговыйЗапрос.УстановитьПараметр("Сертификат", Сертификат);
	РезультатЗапроса = ИтоговыйЗапрос.Выполнить();
	ДолжностьПоСертификату = "";
	Если Не РезультатЗапроса.Пустой() Тогда
		ДолжностьПоСертификату = РезультатЗапроса.Выгрузить()[0].Должность;
	КонецЕсли;
	
	Возврат ДолжностьПоСертификату;
	
КонецФункции

#КонецОбласти
