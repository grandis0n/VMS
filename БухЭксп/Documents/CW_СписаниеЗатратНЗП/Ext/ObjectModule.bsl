
Процедура ОбработкаПроведения(Отказ, Режим)

	// регистр CW_НезавершенноеПроизводство Расход
	Движения.CW_НезавершенноеПроизводство.Записывать = Истина;
	Для Каждого ТекСтрокаСостав Из Состав Цикл
		Движение = Движения.CW_НезавершенноеПроизводство.ДобавитьРасход();
		Движение.Период = Дата;
		Движение.Организация = Организация;
		Движение.ПодразделениеОрганизации = ПодразделениеОрганизации;
		Движение.Грузоперевозка = ТекСтрокаСостав.Грузоперевозка;
		Движение.Контрагент = ТекСтрокаСостав.Контрагент;
		Движение.Договор = ТекСтрокаСостав.Договор;
		Движение.ПриложениеКДоговору = ТекСтрокаСостав.ПриложениеКДоговору;
		Движение.ДокументРасходов = ТекСтрокаСостав.ДокументРасходов;
		Движение.ПорядокОтражения = ТекСтрокаСостав.ПорядокОтражения;
		Движение.СтавкаНДС = ТекСтрокаСостав.СтавкаНДС;
		Движение.СпособУчетаНДС = ТекСтрокаСостав.СпособУчетаНДС;
		Движение.Номенклатура = ТекСтрокаСостав.Номенклатура;
		Движение.СтатьяЗатрат = ТекСтрокаСостав.СтатьяЗатрат;
		Движение.Подразделение = ТекСтрокаСостав.Подразделение;
		Движение.Валюта = ТекСтрокаСостав.Валюта;
		Движение.Покупатель = ТекСтрокаСостав.Покупатель;
		Движение.ДоговорПокупателя = ТекСтрокаСостав.ДоговорПокупателя;
		Движение.ПриложениеКДоговоруПокупателя = ТекСтрокаСостав.ПриложениеКДоговоруПокупателя;
		Движение.Количество = ТекСтрокаСостав.Количество;
		Движение.Сумма = ТекСтрокаСостав.Сумма;
		Движение.НДС = ТекСтрокаСостав.НДС;
		Движение.Всего = ТекСтрокаСостав.Всего;
		Движение.Сумма_Валюта = ТекСтрокаСостав.Сумма_Валюта;
		Движение.НДС_Валюта = ТекСтрокаСостав.НДС_Валюта;
		Движение.Всего_Валюта = ТекСтрокаСостав.Всего_Валюта;
	КонецЦикла;

КонецПроцедуры
