
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Отказ = CW_ДоходыНерезидентовСервер.ПоказыватьРекламуПриСтартеПользователя();
	Если Отказ Тогда
		Возврат;
	КонецЕсли;
	
	Если Параметры.Свойство("СсылкаНаРесурс") Тогда
		ТекстHTMLДокумента = Параметры.СсылкаНаРесурс;
	Иначе
		МакетHTML = ПолучитьОбщийМакет("CW_ДоходыНерезидентовРекламныйБаннер");
		ТекстHTMLДокумента =   МакетHTML.ПолучитьТекст();
	КонецЕсли;
	
	HTMLДокумент =	ТекстHTMLДокумента;
	
КонецПроцедуры

&НаКлиенте
Процедура НеПоказыватьПриСтартеПриИзменении(Элемент)
	
	НеПоказыватьПриСтартеПриИзмененииНаСервере();   
	
КонецПроцедуры

&НаСервере
Процедура НеПоказыватьПриСтартеПриИзмененииНаСервере()
	
	CW_ДоходыНерезидентовСервер.УстановитьПоказыватьРекламуПриСтартеПользователя(НеПоказыватьПриСтарте);	
	
КонецПроцедуры

&НаКлиенте
Процедура HTMLДокументПриНажатии(Элемент, ДанныеСобытия, СтандартнаяОбработка)
	
	ИмяФайла = "https://www.coralway.ru/products/691";
	#Если Вебклиент Тогда 
		ПерейтиПоНавигационнойСсылке(ИмяФайла);
	#Иначе
		ЗапуститьПриложение(ИмяФайла);
	#КонецЕсли	
	
КонецПроцедуры
