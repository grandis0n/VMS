
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Параметры.Свойство("Корректировка",	Корректировка); 
	
	Параметры.Свойство("Количество",	Количество);
	Параметры.Свойство("Сумма",			Сумма);
	Параметры.Свойство("НДС",			НДС);
	Параметры.Свойство("Всего",			Всего); 
	
	Параметры.Свойство("КоличествоДоКорректировки",		КоличествоДоКорректировки);   
	Параметры.Свойство("СуммаДоКорректировки",			СуммаДоКорректировки);
	Параметры.Свойство("НДСДоКорректировки",			НДСДоКорректировки);
	Параметры.Свойство("ВсегоДоКорректировки",			ВсегоДоКорректировки);   

	Параметры.Свойство("НадписьДоКорректировки",	НадписьДоКорректировки);   
	Параметры.Свойство("НадписьПослеКорректирвоки",	НадписьПослеКорректирвоки);   
	
	Элементы.НадписьДоКорректировки.Видимость		= Корректировка; 
	Элементы.СуммаДоКорректировки.Видимость			= Корректировка; 
	Элементы.НДСДоКорректировки.Видимость 			= Корректировка; 
	Элементы.ВсегоДоКорректировки.Видимость			= Корректировка; 
	Элементы.КоличествоДоКорректировки.Видимость	= Корректировка;     
	
КонецПроцедуры
