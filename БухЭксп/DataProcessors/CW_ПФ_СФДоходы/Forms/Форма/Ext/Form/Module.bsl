
&НаСервере
Функция  ПечатьНаСервере()
	
	ТОбъект = РеквизитФормыВЗначение("Объект");
	
	
	Возврат ТОбъект.ПечатнаяФорма();


КонецФункции

&НаКлиенте
Процедура Печать(Команда)
	СтруктураПФ = ПечатьНаСервере();
	Результат = СтруктураПФ.ПечатнаяФорма; 
КонецПроцедуры
